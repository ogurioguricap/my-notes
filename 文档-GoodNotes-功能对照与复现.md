# Goodnotes 功能对照与复现分析

> **对照对象**：Goodnotes（App Store 官方页，版本 7.1.22）+ Goodnotes 6 官方博客《Introducing Goodnotes 6: AI Note-Taking, AI Math Assistance, Dynamic Templates & more》（2023-08-09）
> **被分析对象**：本站 `notes-site`——零依赖、纯静态、无后端的个人笔记站（Markdown 笔记 + GitHub Pages + 可视化编辑器 + 手写标注层 + **本期新增的笔记本模式**）
> **依据文件**：`README.md`、`docs/js/ink.mjs`、`docs/js/notebook/*.mjs`、`docs/css/notebook-*.css`、`tools/test-notebook.mjs`、`docs/data/index.json`
> **状态口径**：状态列来自**逐行核对当前代码**；写到"已交付"的能力均有对应文件与测试，未实现者不写成已实现。

---

## 0. 本期交付摘要

**一句话**：笔记本模式（GoodNotes 模式）已落地并接入路由与测试——资料库、多页笔记本、手写引擎、学习集、录音时间锚定、导出全部可用。

| 项 | 内容 |
| --- | --- |
| 新增视图与路由 | `docs/index.html` 加 `#view-books` / `#view-book`；`docs/js/app.js` 接线 `#/books`、`#/book/<id>`、`#/book`，并 `import` `NotebookStore / LibraryUI / NotebookView` |
| 数据层 | `docs/js/notebook/store.mjs`（笔记本 / 文件夹 / 页面 / 书签大纲 / 闪卡 Leitner 间隔重复 / 录音 + `audioAnchor` 时间锚定 / 回收站 30 天 / 导出导入 JSON / 老数据迁移） |
| 纸张与封面 | `docs/js/notebook/paper.mjs`（14 种纸张模板、8 种尺寸含自定义、纸张颜色与自动线色、封面 12 色 × 6 花纹 + 首字标记） |
| 手写引擎 | `docs/js/notebook/page.mjs`（4 笔型 + 速度与压感笔宽 + 起收笔笔锋、荧光笔自动拉直、橡皮 3 模式、套索自由/矩形 + 移动缩放旋转 + 改色改透明度 + 置顶置底、形状识别 + 填充、文本框、图片、贴纸元素库、和纸胶带 + 撕胶带、激光笔、尺子吸附、放大窗 + 自动推进、视口缩放、跨页撤销重做） |
| 界面与产出 | `library.mjs` + `notebook-library.css`（多选批量 / 文件夹 / 标签 / 回收站 / 备份）、`viewer.mjs` + `notebook-viewer.css`（缩略图栏 / 页面管理 / 大纲 / 闪卡 / 录音 / 演示 / 导出 PDF·PNG·JSON / 打印）、`study.mjs`（自写 PDF 写出器、PNG、MediaRecorder、本地抽取式摘要）、`notebook-base.css` |
| 测试怎么跑 | `node tools/test-notebook.mjs` → **694 项断言，当前全绿**（另有 `test-runtime.mjs` 90 项在最小 DOM 里把界面真跑一遍） |

### 第二十二批交付（页面总览：一屏看完 + 多选批量管理）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **批量页面操作（数据层）** | `store.removePages / duplicatePages / movePages / setPagesBookmark` | 删除遵守「**至少留一页**」的老规矩（全选删除会被拒绝，返回 0）；复制把副本**紧跟各自原页**（从后往前插，避免前插把后插的下标顶偏）；移动是「把我选的这几页整块搬到某位置」（全选时拒绝）；批量书签只统计**真的变了**的页（已加书签的再加不重复计数） |
| **页面总览界面** | viewer 的 `toggleOverview / renderOverview / ovToggle / ovBatch / exportSelectedPagesPdf` | 一屏网格（`auto-fill` 自适应列，手机 88px 起），**点格子选中、Shift 连选**、全选 / 清除；**双击某页直接跳过去**；底部操作条给七个批量动作；没选页时按钮是 disabled（状态与选择同步）。缩略图**分批绘制**（每批 8 张，`setTimeout` 让出主线程），几十页也不会卡 |
| **导出选中页 PDF** | `exportSelectedPagesPdf(qualityId)` | 只导出选中的那几页（保持页序），画质 / 阅读版 / 撕胶带 / 省纸排版都沿用当前设置；**子集导出不放书签、目录与页内链接**——页号会变，放了反而指错（界面上也写明了） |

> **口径说明**：第二十二批**不改变状态统计**（仍是 `✅49 / 🟡13 / 🟠11 / ❌9`）——页面管理原本就在已交付侧，但此前只能**一页页**操作（加页 / 复制页 / 删除页 / 拖拽排序都在缩略图栏里逐页点）。这批补的是「整本视角」：几十页的本子要删中间十页、或把某几页挪到最前，原来得点几十次。
> **实现上的取舍**：总览里**单击 = 选中**、双击 = 跳页（不用勾选框）。理由是总览的主要用途是批量操作；勾选框在手机上太小，而双击手势在桌面与触屏都成立。这个取舍写在界面的提示文案里（「点页面选中，双击跳到那一页」），不让用户猜。

### 第二十一批交付（卡片来源整理 + 修掉「填了名字却建不出笔记本」的 bug）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **来源报告（纯函数）** | `store.orphanCardReport(notebooks)` | 把全库「来源有问题」的卡分成两类：**从没记来源**（no-source）与**来源页已被删**（page-missing），并统计总数、两类各多少、涉及几本；坏数据（没有 pages / study 的对象）直接跳过 |
| **自动找回来源** | `store.findCardPageByText(bookId, card)` + `library.findCardSources()` | 按**卡面文字**在本子里搜（含手写识别结果），**只在唯一命中时才挂**——命中多页或找不到就不动（宁可不挂也不挂错）；太短的卡面（< 4 字）不猜。整理完给出「找回 X 张 · 跳过 Y 张」 |
| **批量清理与挂载** | `store.clearCardsPage / setCardsPage` | 「清掉失效来源」只清来源页已删的那些（卡片本身保留，重复清不会重复计数）；`setCardsPage` 支持一次挂多张，且**目标页不存在时不写**（不造新坏来源） |
| **界面** | 复习中心 →「来源整理…」面板 | 按本分组显示待整理数量与前几张卡面，每组可「自动找回 / 去看看」；顶部给两类计数与「全部自动找回 / 清掉失效来源」两个一键操作 |
| **顺手修掉一个真 bug** | `library._onInput` + `_createBook` + 新增 `_sheetInputValue` | **症状**：新建笔记本时明明写了名字，点「创建并打开」却提示「先给笔记本起个名字」；更隐蔽的是——先写名字、再点封面颜色/花纹，**名字会被静默清掉**（那一步会重绘弹层，而标题只存在「输入框」里、从没同步进状态）。**原因**：`_onInput` 只处理了资料库搜索框，新建弹层的标题输入框没有任何绑定，而 `_createBook` 只读状态里的 `f.title`。**修法**：输入时把值同步进 `sheet.fields`（**不重绘**，否则光标会跑、中文输入法会被打断）+ 创建时**以 DOM 里的实际内容为准**（新增 `_sheetInputValue`），双保险。回归测试用真 DOM 复现了完整路径（输入 → 点花纹 → 创建），**在旧代码上会失败** |

> **口径说明**：第二十一批**不改变状态统计**（仍是 `✅49 / 🟡13 / 🟠11 / ❌9`）——第十七批加了「复习卡来源页」，这批把来源体系补成**可维护的**：来源会随着「删页 / 跨本导入 / 从模板建本」而失效，没有整理入口就只能一张张手点。
> **关于那个 bug**：它属于「静态检查查不出来、但用户一定会撞上」的一类——输入框的值从来没被读进状态，而按钮只读状态。所以修完除了加单测，还在 `test-runtime.mjs` 里用真 DOM 走了一遍「输入 → 点 chip → 创建」，并**验证过它在旧代码上确实失败**（75/77），避免以后回归。

### 第二十批交付（打印检查单 + 导出跳过空白页）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **打印检查单（纯函数）** | `study.printCheck(pages, { layoutId, qualityId, doubleSided, dropTape })` + `printCheckLine` | 导出前把账算清楚：**几页 → 几面 · 双面打印约几张纸**（面数÷2 上取整）· 体积粗估（按画质分档）；逐页度量「有多少手写/多少字/识别了多少」，然后给出四类**值得注意的页**：**完全空白**、**只有胶带**（多半忘写了）、**有手写但没识别**（PDF 里搜不到）、**内容很密**（打印前先看一页）。撕胶带与省纸排版都会算进去（撕完可能多出空白页、2 页拼一张时面数减半） |
| **检查单面板** | viewer 的 `markupPrintCheckPanel` + 菜单「打印检查单…」 | 面板里显示总结句与四类计数，每条警告**列出页号且可点着跳过去**（`printCheckGoto`）；下面直接给「按当前设置导出 PDF」与「跳过空白页」开关 |
| **导出时跳过空白页（含重编号）** | `buildPdf({ dropBlank })` + `pdfFromImages` 的书签 / 内链 / 目录重映射 | 空页不占纸，但**页码会变**——所以书签、目录页里的页码、页内「见第 N 页」链接**一起重编号**：指向被跳掉那页的书签/链接直接丢掉（不留死链），目录页码按「原页序 − 它前面被跳掉的页数」重算。这一条是这批最容易写错的地方，测试用**真的跑一遍 buildPdf** 来验（含目录页文字层里的页码） |

> **口径说明**：第二十批**不改变状态统计**（仍是 `✅49 / 🟡13 / 🟠11 / ❌9`）——「打印与导出」这一层原本就在已交付侧。这批补的是**打印前的最后一公里**：手写笔记最容易出现「这本有 40 页但其中 12 页是空的」，直接打印等于白打三张纸，而且页号一乱书签全指错。
> **实现上的取舍**：**不做自动跳过**（空白页也可能是你故意留的空白，比如要手写补充），只提供**一键开关 + 清楚的提示**；也**不做「自动删掉空白页」**这种破坏性动作——导出是导出，改内容归编辑。

### 第十九批交付（PDF 书签落到页内位置）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **落点启发式（纯函数）** | `study.pageAnchor(page, { title })` | 优先级：**对得上页标题的文本框** → 该页最靠上的文本框 → 最靠上的任何对象 → OCR 第一行的行框 → 什么都没有则返回 `null`。胶带不算内容（否则书签会被胶带拽到页尾）；坐标一律夹在 0~1，坏数据不会写出离谱的 y |
| **挂到书签上** | `study.attachOutlineAnchors(outlines, pages)` + `buildPdf` 默认调用 | 有内容的页带上 `at`，空白页不带（各自退回 `/Fit`）；不动原数组，只返回新条目。`buildPdf({ outlineAnchors: false })` 可以整体关掉 |
| **写进 PDF** | `pdfFromImages` 的大纲分支 | 带 `at` 的写 `/Dest [page /XYZ x y null]`：`y = 格位 y + (页高 − 归一化 y × 页高) × 缩放`（**拼版时同样成立**）；靠左对齐（x ≤ 0.25）时 x 写 `null`，让阅读器保持横向位置，分栏/靠右才给具体 x；不带 `at` 的仍写 `/Fit` |
| **顺手修掉一个真 bug** | `outlineTree` | 它原来会**丢掉额外字段**（只保留 title / pageIndex / level / children），所以落点根本传不进 PDF——现在改为展开原条目再覆盖规范字段。这类「数据在中间层被悄悄抹掉」的问题，只有把 PDF 字节读回来才看得出来 |

> **口径说明**：第十九批**不改变状态统计**（仍是 `✅49 / 🟡13 / 🟠11 / ❌9`）——「PDF 导出」这一层原本就在已交付侧。这批把它从「能用」推到「好用」：一页写满手写时，书签原来只跳到页首，现在直接落到那一行。
> **为什么值得做**：长页面（一页手写几十行）+ 大纲跳转是复习/打印时最常用的组合。跳页首意味着每次都要自己再滚一段；跳页内位置才算真正「点一下就到位」。测试直接用字节级断言锁住换算：0.1 的落点在 842pt 页上必须写出 `757.8`。

### 第十八批交付（跨页拖拽：把对象拖到缩略图上就换页）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **拖出去也能知道指哪一页** | `page.mjs` 的 `onSelectionDrag / onSelectionDrop` 钩子 + viewer 的 `thumbIndexAtPoint`（`document.elementFromPoint`） | 画布按住了指针（`setPointerCapture`），所以拖到画布外指针事件仍然回到画布——于是就地在 `pointermove` 里把指针位置报给宿主，宿主用 `elementFromPoint` 反查缩略图（`data-act="thumb"`），命中就高亮（`.nb-thumb.drop`）。拿不到 `elementFromPoint` 的环境安全返回 -1，不影响正常拖动 |
| **落页：一次搬走选中的对象** | `page.mjs` 的 `moveSelectionToPage(targetIndex)` + `mergeItemsInto / itemIdPrefix`（纯函数） | 把选中对象从当前页摘掉、并到目标页末尾（**id 撞车时换新 id**，避免两页合起来出现重复 id；坐标是 0~1 相对值，换页后位置不变）；两页都立刻落盘，不会只写一半 |
| **一步撤销（这次的重点）** | `page.mjs` 的跨页历史：`history.push('移到第 N 页', { pages: [...] })` + `_historyState` 的同伴页扩展 + `_applyHistoryState` 的多页分支 + 宿主钩子 `onApplyPageItems` | 普通历史条目只记「一页 + 操作前内容」，跨页移动天然要改两页。所以这条历史存**两页各自的操作前内容**：一次撤销就把两页同时复原（源页内容回来、目标页回到原样），不会出现「撤销后对象还在目标页（重复）」；重做同样对两页生效（`_historyState` 会把同伴页的当前内容一起带进重做栈） |
| **界面** | viewer：`dragOverThumb / highlightThumb / dropOnThumb / insertItemsAtPage / writePageItems` + 样式 `.nb-thumb.drop` | 松手落页后提示「已把选中的对象移到第 N 页（Ctrl/Cmd+Z 可撤销）」；拖拽中目标缩略图有强调描边 |

> **口径说明**：第十八批**不改变状态统计**（仍是 `✅49 / 🟡13 / 🟠11 / ❌9`）——「对象编辑」这一层原本就在已交付侧。这批补的是编辑动作里最常用却一直缺的一个：**把内容挪到别页**。原来只能「复制 → 翻页 → 粘贴 → 回原页删掉」，现在一步到位。
> **实现上的取舍**：跨页拖拽没有做成「拖起一个幽灵跟随光标」（那需要重做一套拖拽渲染层），而是**复用画布已有的选中拖动**：拖到缩略图上松手即落页。好处是不新增手势体系、移动端也一样能用（手指拖到缩略图即换页），代价是落点判定依赖缩略图在屏幕上的位置（`elementFromPoint`）——所以测试里两种环境都验过（有/没有 `elementFromPoint`）。

### 第十七批交付（复习卡的来源页：从卡回到原文）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **来源解析（纯函数）** | `store.cardSourceOf(notebook, card)` + `dueQueue` 条目补 `pageIndex / pageTitle / sourceOk` | 卡片本来就存着 `pageId`（第五批导出 Markdown 时开始存的），但一直没人用。现在统一解析成「第几页 / 页标题 / 是否还在」：**页序跟着页面顺序走**（删掉前面一页，来源页序自动跟着变）；来源页被删则 `ok=false` 但仍保留 `pageId`，界面才能说清「来源页已被删除」而不是假装没有来源 |
| **卡面回到原文** | `library._sheetReviewSession` 的「来自《…》第 N 页 · 页标题」+「去看原页」；`viewer.markupStudyPanel` 的卡面与卡片列表 | 资料库复习会话里点「去看原页」跳到那一页看原文，**回到资料库还是同一张卡**（会话状态留在内存里，不重开队列）；本子里的学习集面板同样有「去看原页」，卡片列表每行显示「第 N 页」并可点 |
| **来源坏掉的处理** | `store.setCardPage` + `store.orphanCards` + 会话里的「清掉来源」 | 页被删过、或从别处导入来的卡可能是「孤儿来源」：显示「来源页已被删除」并给「清掉来源」；`orphanCards` 能把它们列出来（后续做「批量整理来源」有据可依）。`setCardPage` 传不存在的页 id **不会写进去**，避免造出新的坏来源 |

> **口径说明**：第十七批**不改变状态统计**（仍是 `✅49 / 🟡13 / 🟠11 / ❌9`）——闪卡本身记在已交付侧。这批补的是学习闭环里缺的一环：复习时看到一张卡，**想知道它是从哪段笔记里出的、想回去看上下文**，原来只能自己在笔记本里翻。现在从卡面一键回到原文，回来还在原位接着复习。
> **为什么值得做**：间隔重复的价值在于「卡片要能独立成题」，但学习者的真实动作往往是「卡面想不起来 → 回原页看上下文 → 回来接着背」。把这一步做顺，闪卡才不只是个孤立的背诵列表。

### 第十六批交付（识别质量回看清单：哪几页值得重跑）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **质量判定（纯函数）** | `ocr.inkUnits / lineGarbageRatio / duplicateLineRatio / OCR_QUALITY_RULES / ocrQualityOf` | 先把「这一页有多少手写」量出来（笔迹点数 / 文本字数 / 图片贴纸，胶带不算），再机械判六种可疑长相：**字数异常少**（识别出的字 < 12 而手写量 ≥ 30）、**疑似乱码**（平均乱码比例 ≥ 0.5）、**内容重复**（重复行 ≥ 40%，同一个幻觉抄好几遍）、**缺少行位置**（≥3 行但带框的不足 30%）、**行太碎**（≥6 行且平均每行不到 2.5 字）、**上次失败**。每条规则带「人话解释」，界面直接取用，避免两处口径不一致 |
| **全库清单** | `ocr.ocrQualityReport / qualityPageIds` + `library._sheetOcrQuality` | 扫全库只列 `差 / 疑` 的页（正常页、确认没字的页不进清单），按「差 → 疑」排序、按理由汇总计数；每行显示本名 / 页号 / 理由 / 识别出多少字多少行 / 手写量估算，**点「去看看」直接跳到那一页**（`app.js` 新增 `pendingBookPage` + `viewer.gotoPage`） |
| **一键只重跑这些页** | `planOcrQueue({ force })` + `ocrQueue({ force })` + 清单里的两个按钮 | 「已识别过就不重跑」是第十二批的省钱规矩，但质量回看要的正是**重跑**——于是加了一条明确通道：`force` 列表里的页即使 `done` 或 `blank` 也会再跑一遍（状态标 `redo`），其余页依旧跳过。两个按钮分别是「重跑这些 N 页」与「**重跑并补行位置**」（顺手打开行框识别，专治「缺少行位置」） |
| **闭环** | 判定 → 抽查 → 重跑 → 复判 | 重跑后质量按新结果**重新判定**（测试里验过：假接口先返回两个字 → 判「差」，重跑返回一整行 → 不再可疑）；不带 `force` 再跑一次是**零请求**，重跑只在你点的时候发生 |

> **口径说明**：第十六批**不改变状态统计**（仍是 `✅49 / 🟡13 / 🟠11 / ❌9`）——手写识别这条本来就是 🟡（依外部模型、需手动触发）。这批解决的是「**识别质量看不见**」这个实际问题：批量跑完之后你没法判断哪些页白花钱了，只能一页页翻。判定规则刻意做成**保守 + 可解释**：宁可少报（正常页不报警），每条都写明「为什么可疑」和「怎么修」。
> **设计取舍**：没有做「自动重跑」（花钱的事必须人点），也没有做「按分数排序自动挑最好的模型」（两个模型都跑一遍 = 双倍花费）。清单只回答一个问题：**这几页值得你花一次钱再跑一遍吗？**

### 第十五批交付（省纸排版：2 页拼一张 / 骑马钉小册子）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **排版档位** | `study.PDF_LAYOUTS / layoutOf / layoutHint` | 四档：**一页一张**（默认，保持原始页面大小）、**2 页拼一张（A4 横版左右并排，字号约 71%）**、**2 页拼一张（A4 竖版上下，约 50%）**、**骑马钉小册子（A3 双面折成 A4）**。提示文案会算给你看：「8 页 → 4 面（双面打印约 2 张纸）」 |
| **拼版几何（纯函数）** | `study.planSheets(sizes, { layoutId, gap, margin })` | 把「每页多大」算成「每张纸怎么放」：纸的尺寸、每张几格、每格放在哪、等比缩放多少（按 `min(格宽/页宽, 格高/页高)` 适配并居中，**不裁切不拉伸**）。一页一张时纸张尺寸**跟着每一页自己走**，混排不同尺寸的页也不会互相污染 |
| **骑马钉页序** | `study.bookletOrder(count)` | 骑马钉装订的页序自动重排：4 页 → 正面 `[4,1]`、背面 `[2,3]`；8 页 → `[8,1] [2,7] [6,3] [4,5]`。不足 4 的倍数时**越界记 0（空白格不画）**；测试逐个数过「每页只出现一次」 |
| **拼版后的 PDF 仍然完整** | `pdfFromImages({ layoutId })` 重写为「按纸写页」+ 坐标系变换 | 每格用 `q <缩放> 0 0 <缩放> <x> <y> cm … Q` 把坐标系挪到那一格，再按**原始页坐标**画图和文字层——所以 **可搜索文字层不用重排**；书签目录的 `/Dest` 指向「那一页所在的纸」；**页内链接的矩形跟着缩放与格位偏移换算**；`/MediaBox` 与 `/Count` 都按纸算。`readTextLayer` 自检、xref/startxref 偏移仍逐项通过 |
| **界面** | viewer「导出 ▾」新增一组**排版**开关（带 ✓ 标记，切换后就地更新不用重开菜单）；资料库批量导出的 sheet 也有同一套 chips | 文件名会自动带 `-nup2h` 这类后缀，导出提示写「N 页 → M 面 · 排版 2 页拼一张（A4 横版）」 |

> **口径说明**：第十五批**不改变状态统计**（仍是 `✅49 / 🟡13 / 🟠11 / ❌9`）——「打印与导出」这一层原本就记在已交付侧，这批补的是**唯一还没做的打印/排版能力**：省纸。它是「替代方案」精神的延续：不追求和 Goodnotes 一模一样，而是把「打印出来带走」这件事做到能用（双面打印省一半纸，骑马钉直接出小册子）。
> **实现上的一个取舍**：拼版没有「重新渲染一遍每页」，而是在 PDF 内容流里用变换矩阵把原始页整体缩放平移——好处是**文字层、内链、书签、图**全都自动跟着走，代价是没法在拼版时改页面样式（那本来就该在阅读版 / 纸张设置里做）。

### 第十四批交付（EPUB 补齐：层级目录 / 书封 / 书内链接 / NCX）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **层级目录** | `study.navListHtml / outlineTree`（复用 PDF 那份 `outlinesFromNotebook`）+ `buildEpub({ toc })` | 有标题的页当一级、只设书签的页挂到它下面当二级，目录用**嵌套 `<ol>`** 表达（EPUB3 规范就是这么表达层级的）；一本没标题也没书签的本子退回「一页一条」的平铺目录，不会出现空目录 |
| **EPUB2 的 `toc.ncx`** | `study.ncxFromOutline` + `buildEpub` 写 `OEBPS/toc.ncx` 并在 spine 上标 `toc="ncx"` | 老阅读器（只认 NCX 的那批）也能看到目录；`navPoint` 嵌套表达层级，`playOrder` 是按先序编号的**唯一**序号。这里修掉一个真 bug：父节点的序号不能等拼串时再读（子节点递归会先把计数器推上去，导致父子同号） |
| **书封** | `study.epubCoverArt` + `buildEpub({ cover })` + viewer 传 `renderCoverImpl: renderCover` | 优先用**你自己的封面图**（按 data URL 的 mime 选扩展名），没传就按**配色封面画一张**（复用 `paper.renderCover`，同一套样式）；封面页放在 spine 最前面，manifest 标 `properties="cover-image"`（EPUB3）+ `<meta name="cover">`（EPUB2）。封面图坏了/画不出来就不放封面，导出不会因此失败 |
| **书内链接** | `study.linkifyPageRefs` + `buildEpub({ links })` | 正文里写「见第 3 页」「P12」会变成 `<a href="p3.xhtml">` 的书内链接（点一下就跳过去），并且**指到本子外面的页号不生成链接**（避免死链）；链接文字照样转义，正文里的尖括号不会被当标签 |
| **结构自检（测试里做的）** | `tools/test-notebook.mjs` 第 18 节 | 除了逐项断言，还**解开自己写出来的 ZIP** 逐条核对：`mimetype` 必须第一条且不压缩、每页 xhtml/jpg 数量、spine 顺序与页序一致、中文仍是 UTF-8（阅读器搜得到手写识别内容），以及**所有 `href`/`src` 都指向包里真实存在的文件**（死链检查） |

> **口径说明**：第十四批**不改变状态统计**（仍是 `✅49 / 🟡13 / 🟠11 / ❌9`）——EPUB 导出本身记在「分享 / 导出」的替代路径里，这批把它从「能打开」做到**像一本正经电子书**：有书封、有层级目录、目录点得动、正文交叉引用点得动、老阅读器也有目录。顺带把「死链」这类只有真解开包才能发现的问题放进了测试。
> **为什么值得单独做**：EPUB 是**唯一一个「发出去之后你控制不了阅读器」的格式**——PDF 至少还能假设 Adobe/系统阅读器，EPUB 的用户可能在用十年前的设备。所以这批的重点不是加功能，而是把规范里的**兼容层（NCX / meta cover / 嵌套 ol）**和**链接完整性**补齐。

### 第十三批交付（复习中心：筛着复习 / 复习热图 / 导出 Anki）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **卡片标签** | `store.parseCardTags / addCard({ tags }) / setCardTags` + `normalizeCard`（老数据自动补 tags 字段）+ 笔记本面板「加卡片」多一个标签输入框 | 标签可以用逗号 / 顿号 / 分号分隔，去重、去空、最多 8 个；老卡片加载时自动补齐形状，不会因为新增字段炸掉 |
| **筛着复习** | `store.dueQueue({ tag, bookTag, bookId, folder, limit, includeNotDue })` + `store.dueFacets()` + `library.startReviewSession / _reviewStep / _sheetReviewCenter / _sheetReviewSession` | 复习中心里按 **卡片标签 / 笔记本标签 / 某一本** 筛：每个筛选按钮后面直接写着还剩几张到期（`dueFacets` 一次算完）；点「开始复习」把筛选结果**复制成队列**（复习过程中卡的到期时间会变，队列不变），按**盒子层数从小到大**排——最不熟的先来。「忘了」记 lapse 并在 10 分钟后再出现；「跳过」不动记账；一轮跑完给「记住 N / 忘了 M」的收尾页，可以再开一轮 |
| **复习热图** | `store.logReview / reviewHeat / dayKey / parseReviewLog / REVIEW_LOG_DAYS` + `library._heatHTML` + `.lib-heat` 样式 | 每次复习都按**本地日期**记一笔（分记住 / 忘了），只保留最近 400 天；热图 = 最近 26 周、一天一格、5 档颜色（0 / 1~4 / 5~14 / 15~29 / 30+），带「总次数 / 有复习的天数 / 当前连续 / 最长连续」；今天还没复习不算连胜中断（不然早上打开就显示断签）。数据跟着备份走，**导入备份时按天取较大值合并**，不会因为导两次就翻倍 |
| **导出 Anki CSV** | `study.cardsToAnkiCsv / csvField / ankiHtml / ankiCsvFromStore / ANKI_HEADER` + 复习中心「导出 Anki CSV / 只导到期」 | 三列（正面, 背面, 标签）+ Anki 需要的三行文本头（`#separator:Comma`、`#html:true`、`#tags column:3`）与 `#deck:` 牌组名；字段按 CSV 规矩转义（逗号 / 引号 / 换行），卡面里的换行转成 `<br>`、HTML 字符转义（卡面不会被当 HTML 执行）；标签列自动带上卡自己的标签 + `本_笔记本名`，导入后能按「本」筛；没有正面的卡直接跳过 |

> **口径说明**：第十三批**不改变状态统计**（仍是 `✅49 / 🟡13 / 🟠11 / ❌9`）——Goodnotes 的学习统计/复习功能本来就记在「已交付」侧，这一批是把闪卡从「能复习」推到「筛着复习 + 有统计 + 能带出去」。导出 Anki 属于**加分项**（Goodnotes 自己没有）：「导出来带到别的工具继续练」这条路原本只有 Markdown，现在补上了间隔重复生态的标准交换格式。

### 第十二批交付（跨本批量识别队列：可续跑 / 只重试失败页 / 不重复花钱）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **跨本识别队列** | `ocr.mjs` 的 `planOcrQueue / pageOcrState / pageHasInk / ocrQueue / queueStats / resumeQueue / isFatalOcrError / queueProgressText` + `store.enqueueOcr / dequeueOcr / queuedOcrBooks / recordOcrJob` | 资料库多选几本 → 「识别手写」：一次把多本本子的待识别页排成一队跑。**状态机**把一页分成 `pending / done / blank / failed` 四种，队列只挑还需要跑的页——于是「已经识别过的页」和「上一轮判定为『这页没字』的页」都不会再花钱；`retryFailed` 控制失败页要不要一起重试，`max` 支持小步试跑，`includeEmptyPages` 才允许把纯空白页也送去识别。并发池与按页回写都复用第八批那套 |
| **断点续跑（不需要额外保存进度）** | 每页识别完立刻 `setPageOcr` / `markPageBlank` / `setPageOcrError` 落盘；入队标记 `nb.ocrQueued` 与任务记录 `nb.ocrJob` 都存在笔记本本体上 | 关掉页面、刷新、隔天再打开都不白跑：资料库顶部出现「**手写识别队列还剩 N 页**」（含涉及几本、其中几页上次失败）→ 点「继续识别」从剩下的页接着跑，「移出队列」则不再提醒（已识别的文字保留）。因为入队与进度写在笔记本对象里，**导出备份 / 同步到仓库时会一起走** |
| **失败可追溯 + 只重试失败页** | `store.setPageOcrError / clearPageOcrError`（记原因、次数、时间）+ 资料库「只重试失败的 N 页」+ 笔记本面板逐页列出「第几页 / 为什么」 | 识别失败不再只是一句 toast：原因落在那一页上，面板里列得出来，也能一键只重跑这些页；重试成功后自动清掉失败痕迹。单页/整本识别（第八批的入口）也改成失败留痕，两条入口共用同一套记账 |
| **致命错误短路** | `isFatalOcrError` + `ocrQueue({ fatalStreak })` | 密钥无效 / 余额不足这类「再试也没用」的错误会**立刻停住整个队列**（测试断言：5 页的本子试了不到 5 次就停，剩下的页保持待识别状态等下次），不会被几十页的必败请求耗光时间与额度；连续 3 页失败同样停，原因写进任务记录 |
| **界面** | `library.mjs` 的「识别手写」按钮 + 队列小面板（模型 / 并发 / 重试开关 / 预估耗时 / 进度条）+ 续跑横幅 + 进度面板（停止、只重试失败页）；`viewer.mjs` 面板补「重试失败的 N 页」与「几页确认没字」 | 预估耗时按模型与并发粗算（8B ≈ 20 秒/页、32B ≈ 70 秒/页）；面板文案把三条「不浪费」规则直接写给人看；样式在 `notebook-library.css`（`.lib-resume` / `.lib-bar`）。测试除了 39 项数据层断言，还在 `test-runtime.mjs` 里用最小 DOM 真渲染一次横幅与面板（写错会整块资料库白屏，这类问题静态检查查不出来） |

> **为什么值得单独做一批**：手写识别是**要花钱的外部调用**，所以「同一页别跑第二遍」和「中断能接着跑」比「多识别几页」更重要。第十二批把 OCR 从一个按钮变成一个**有状态、可中断、可续跑、会记账的小队列**；顺带把「这页没字」也变成被记住的状态，避免每次批量都把空白页再烧一遍 token。状态统计仍为 `✅49 / 🟡13 / 🟠11 / ❌9`——手写类能力仍记在 🟡（依外部模型、需手动触发），本批是把它做扎实，不是新增等价能力。

### 第十一批交付（识别后原位替换 / 自定义封面图 / 今日复习入口 / EPUB·单文件 HTML）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **识别结果原位替换笔迹** | `page.mjs` 的 `planInkToText` + `PageEditor.replaceInkWithText` + viewer OCR 面板「把识别结果换成文本」 | 这是「手写转文字」那条 🟡 的推进：不再只是把手写**另存**成文本框，而是**按行框把覆盖到的笔迹删掉、在原位放下同尺寸的可编辑文本框**。计划是纯函数：只删「中心点落进行框」的笔迹（贴纸/胶带/图片/文本框不动），字号 = 行高 × 0.82 并夹在 0.01~0.06 之间，颜色沿用纸张线色；`removeIds` 与 `texts` 一起返回，测试逐条核对删除集与定位，并验证整批操作在撤销栈里只算**一步**（撤销后笔迹全回来） |
| **自定义封面图** | `store.mjs` 的 `setCoverImage / clearCoverImage` + `library.mjs` 卡片菜单「换封面图 / 移除封面图」 | 封面从「配色 + 花纹 + 首字」升级为**可以用自己的图**：图片以 data URL 存进笔记本本体（所以导出 JSON / 同步仓库 / 备份都带着它），CSS 按封面比例裁切，`has-image` 时隐藏花纹层；可以移除退回配色封面；**只影响显示**，不参与 PDF 导出（导出走纸张模板） |
| **今日复习入口** | `store.mjs` 的 `dueStats / dueNotebooks` + `library.mjs` 的 `.lib-review` 横幅 + `startReview()` + `app.js` 的 `pendingBookPanel` | 资料库顶部显示「今天该复习 N 张卡 · 涉及 M 本」，按「同一盒子里最近到期」排序，点「开始复习」直接进那一本并**自动弹出闪卡面板**（进本子后弹面板的能力由测试守着）。全部复习完横幅自动消失；没用过闪卡的人不会看到横幅 |
| **EPUB / 单文件 HTML 导出** | `study.mjs` 的 `crc32 / zipStore / buildEpub / buildSingleHtml / collectRenderablePages` + viewer 导出菜单 | 两种「发出去就能看」的格式，**全部在浏览器里手写字节**：EPUB 是自写的最小 ZIP（存储不压缩，`mimetype` 必须是第一条且不压缩）—— 每页一个 xhtml（图 + 这一页的文字与手写识别）+ `nav.xhtml` 目录 + `content.opf` 元数据（标题/作者/语言 `zh-CN`），任意阅读器可打开且**手写识别的内容可以搜**；单文件 HTML 把每页 JPEG 内联成 data URL，双击即开、带打印分页样式。测试校验 CRC32 标准值（`123456789 → CBF43926`）、ZIP 本地头/EOCD 签名与条目数、EPUB 每页 xhtml 与 jpg、UTF-8 正文可搜、书名与语言，以及「没有图也不崩」 |

> **口径说明**：第十一批**不改变状态统计**（仍是 `✅49 / 🟡13 / 🟠11 / ❌9`）——「手写转文字」即使做到原位替换，也仍需**手动点一次**（不是系统级实时转换），所以留在 🟡；封面图、复习入口、EPUB/HTML 都是对已判 ✅/🟠 条目的**加厚**，没有新增「与 Goodnotes 完全等价」的行。

### 第十批交付（大纲层级 / 长录音分片 / 命中高亮 / 页面朗读）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **PDF 大纲层级** | `study.mjs` 的 `outlineTree / outlineFlat` + `pdfFromImages` 的 `/Outlines` 写入 | 有标题的页做一级、只设书签的页挂到它下面当二级（同级用 Next/Prev 串、父级写 First/Last/Count）；中文标题仍走 UTF-16BE。测试校验父子对象号、同级链、根 Count 与对象偏移 |
| **长录音分片转写** | `asr.mjs` 的 `planChunks / encodeWavBytes / encodeWavBlob / decodeWithAudioContext / sliceAudio / transcribeChunked` + viewer 的自动分片 | 超过 150 秒自动走分片：浏览器解码成 PCM → 按 2 分钟切 → **自己写 WAV 头**编码成可上传文件 → 逐片转写；每片的时间区间**偏移回原录音**，所以拿到的是逐片精确时间（片内再切句按时长等分）；分片失败自动退回整段转写 |
| **页面朗读（TTS）** | 新模块 `tts.mjs`（`splitSpeakSentences / pageSpeakText / makeSpeaker`）+ 录音面板「🔊 朗读本页」 | 用浏览器自带 `speechSynthesis` 把这一页的文字 + 手写识别逐句读出来，当前句高亮、点某句从那句接着读；不支持时给提示而不是报错；完全离线可用 |
| **命中定位高亮** | `page.mjs` 的 `findHitRects / flashRects / drawFlash` | 内搜跳页后把命中处**黄框标出**（文本框按字符前缀宽度算、手写行按行框比例切），一两秒后自动消失；空查询与无命中都安静返回 |

### 第九批交付（阅读版 PDF / 自动目录页 / 搜索键盘流 / 转写分段锚定）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **阅读版 PDF（省墨）** | `paper.mjs` 的 `renderPaper({ plain, flat })` + `page.mjs` 的 `renderPage({ plain, flat })` + 导出菜单开关 | `plain` 只铺底色、不画格线横线（打印讲义省墨）；`flat` 再强制白底。测试用绘制调用断言「plain 下没有 stroke」 |
| **自动目录页** | `study.mjs` 的 `tocEntries / estimateWidth / renderTocPage / pdfPageCount` + `buildPdf({ toc })` | 导出时在最前面插一页 canvas 画的目录（标题 + 虚线引导 + 页码）：条目带**隐形文字层**（Ctrl+F 能搜目录）与**可点链接**（点标题跳页）；页码已把「目录页占第 1 页」算进去；页数太多放不下就停（不硬挤）；可关闭 |
| **笔记本内搜索键盘流** | `viewer.mjs` 的 `openBookSearch / searchMove / searchGo` + `handleKey` | **Ctrl/Cmd+F** 直接开面板并聚焦；**↑↓** 在命中间移动（高亮当前行）；**Enter** 跳页；**Esc** 关闭 |
| **转写分段 + 每句锚定到页** | `asr.mjs` 的 `parseAsrSegments / anchorSegments / segmentAtTime / transcribeAudioFull` + `store.setAudioSegments / audioSegmentAt` + 录音面板与播放高亮 | 接口给时间戳就用原值（`exact`），没给就按句切、按时长**等分**并在界面上注明「近似」；每句用 `store.audioAnchor` 挂到**说它时所在的那一页与第几个对象**；播放时高亮当前句并自动翻到对应页；点句子也能跳页 |

### 第八批交付（捏合锚点 / 笔记本内搜索 / PDF 内链 / 录音转写）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **捏合以手指中点为锚点** | `page.mjs` 的 `pinchAnchor` + `_bindCanvas` | 画布是 `translate(x,y) scale(s)`，让归一化位置 n 的屏幕坐标在缩放前后不变即得 `x' = x + n·rectW·(1−k)`；测试里有真实不变量断言（缩放前后锚点屏幕位置一致，误差 < 1e-6）。此前只是纯缩放，放大后手指按的位置会跑 |
| **笔记本内搜索** | `viewer.mjs` 的 `markupSearchPanel / renderBookSearch` + 顶部 🔎 | 在本子里搜文字 / 手写识别 / **录音转写**，按页列出、标出来源、关键词高亮（先转义再包 `<mark>`，无注入），点一下跳页并关面板 |
| **PDF 内链** | `study.mjs` 的 `findPageRefs / pageRefLinks` + `pdfFromImages({ linkPages })` | 页面上写「见第 3 页」「P12」这类引用会变成 PDF 链接注解（`/Annots` + `/Subtype /Link` + `/Dest`）：文本框里的引用按字符前缀宽度算矩形，手写行按行框内的字符比例切一段；指到本子外面的引用不生成；可整体关掉 |
| **录音转写（ASR）** | 新模块 `asr.mjs` + `store.setAudioText / audioStats` + 录音面板「转文字」 | SiliconFlow 的 `/v1/audio/transcriptions`（multipart 上传，与 OCR 共用同一家的 Key）。转写结果挂在录音记录上，于是 `searchText / searchAll / snippetOf` 都能命中录音内容——**对照文档里「音频转写 ❌ 无语音模型」这条到此变成 🟡（需自备 Key，音频会上传第三方）**。响应解析兼容 `text/result/results/segments`；「认得出的结构但内容为空」（录音里没人说话）返回空串交给界面提示，结构完全认不出才报错 |

### 第七批交付（PDF 目录 / 长图 / 选区识别 / 移动端手势）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **PDF 书签目录** | `study.mjs` 的 `pdfTitleHex / outlinesFromNotebook` + `pdfFromImages({ outlines })` | 导出时把「书签页 + 有标题的页」写成 PDF `/Outlines`（`/PageMode /UseOutlines`，阅读器左侧直接跳页）；中文标题走 UTF-16BE+BOM，不会乱码；没有书签就不硬凑目录（保持干净），需要时可以按页编号。带目录后对象偏移与 startxref 仍逐项校验通过 |
| **多页长图导出** | `study.mjs` 的 `longImageLayout / fitLongImage / buildLongImage` | 整本竖排拼成一张 PNG（发聊天/贴群）；宽度取最宽页、间距可设；页数太多导致高度超画布上限时**自动降倍率**（把间距也算进去，保证一定不超限） |
| **选区 OCR** | `page.mjs` 的 `selectionCropGeometry / selectionBounds / selectionDataUrl` + 套索面板「识别选区」 | 只把圈中的那一块渲染成白底图（只画选中的对象，避免格线干扰识别）→ 交给 Qwen3-VL → 结果**直接落成可编辑文本框**（贴在选区上方，可拖动、可撤销）。比整页识别更省 token、也更准 |
| **移动端手感** | `page.mjs` 的 `pinchScale` + `_bindCanvas` 双指捏合；`viewer.mjs` 的 `toolsMore` 两段式工具条与 `bindLongPressTools` 长按切笔型；`notebook-viewer.css` | 手机（≤860px）工具条两段式：常用一排 + 「⋯」展开贴纸/胶带/激光笔/尺子；**双指捏合缩放**页面（0.4×~4×，捏合时不落笔）；**长按画笔/荧光笔**循环切笔型（圆珠笔→钢笔→毛笔→铅笔） |

### 第六批交付（OCR 行位置 / 模板库 / Markdown 导出）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **OCR 带行位置** | `ocr.mjs` 的 `OCR_BOX_PROMPT / parseOcrLines / linesToText`、`store.setPageOcr`（存 `page.ocr.lines[].box`）、`study.textRunsForPage` | 识别时让模型返回 `{"lines":[{text,box}]}`（0~1 相对比例），存回页面；导出 PDF 时按行框原位摆放，并按框宽/字宽算 `Tz` 水平缩放，所以**选中/复制会贴着原来的文字位置**。解析器把模型的常见毛病都兜住了（前后夹话、代码围栏、box 反了、越界、缺 box、框太小），解析不出结构化行就退回纯文本行铺（搜索能力不丢）。面板里可关掉「同时识别行位置」 |
| **笔记本模板库** | `store.mjs` 的 `BUILTIN_TEMPLATES / templates / saveAsTemplate / removeTemplate / createFromTemplate` + 资料库新建面板与「模板库…」 | 内置 4 套（康奈尔课堂笔记 / 周计划 / 错题本 / 读书笔记，含纸张与页数）；任何一本可「另存为模板」（可选「含内容」）；新建时可套用模板；自建模板可删、内置的删不掉。对照文档里「可导入模板本」这个 🟡 缺口到此补上 |
| **导出 Markdown（进全站检索）** | `study.mjs` 的 `notebookToMarkdown / markdownSlug / markdownTarget` + 笔记本「导出 Markdown」与「把 Markdown 发到仓库」+ 资料库卡片菜单 | 生成的 `.md` 含 frontmatter（标题/分类=笔记本/标签/日期/摘要）+ 每页小节（文本框内容 + 手写识别）+ 学习集闪卡表格（竖线转义）。下载即可用；也可以一键提交到 `content/<标题>.md` 并用 `applyEdit` 重建 `docs/data/index.json`，**于是手写笔记本变成站点里的一篇笔记**，进资料库、全文检索与知识图谱（测试里真的喂给了站点的索引构建器验证） |

### 第五批交付（PDF 可搜索文字层 / 批量导出 / 搜索结果缩略图）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **PDF 隐形文字层** | `study.mjs` 的 `textCodeMap / charsOfRuns / encodeTextHex / toUnicodeCMap / textRunsForPage / textContentStream / readTextLayer` | 导出的 PDF 现在**能 Ctrl+F、能选中复制**：文本框按它自己的位置/字号/对齐摆位（PDF 坐标已从左上角换算到左下角），已 OCR 的手写按行铺在页面左侧（视觉模型不给坐标，所以只为可搜索/可复制）；整本文档共用一张编码表（Identity-H，2 字节码）+ 自写 ToUnicode CMap（bfchar 每块 ≤100 条，符合规范），文字用 `3 Tr` 隐形绘制；`readTextLayer()` 能把文本层读回来做自检 |
| **批量导出** | `study.mjs` 的 `collectNotebookPages / buildPdfFromNotebooks` + 资料库多选条「导出 PDF」 | 勾选多本 → 小面板选画质（三档）与输出方式（**合并成一个 PDF** / **每本一个 PDF**）→ 逐本下载带 350ms 间隔与进度提示；合并时保持「按本、按页」顺序，逐本时文件之间不会串内容 |
| **搜索结果缩略图** | `app.js` 的 `paintBookThumbs` + `.result-thumb` 样式 | 首页搜索的笔记本结果左侧直接画出**命中那一页的缩略图**（最多 6 张，避免拖慢搜索）；缩略图用的是「导出/打印同一套渲染」，所以和笔记本里看到的一致 |

### 第四批交付（全局搜索合并 / 打印级导出）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **首页全局搜索合并笔记本** | `app.js` 的 `runSearch` + `store.excerptAround/countHits` | 原来首页搜索只搜 Markdown 笔记；现在结果下方多出一段「笔记本（含手写识别）」，按笔记本聚合、显示命中处数、摘要围绕关键词截取并高亮，点一下直接跳回那一本（`.result-book`，左侧色条用封面配色）。只读探测 `peekBookStore()` 保证「没用过笔记本的人不会被凭空建一份空资料库」 |
| **打印级 PDF 导出** | `study.mjs` 的 `PDF_QUALITY / qualityOf / preloadImages / buildPdf` + viewer 的导出菜单 | 三档画质（96 / 192 / 288 dpi，JPEG 质量 0.86/0.9/0.92）；**导出前预加载页面里的图片**（否则 PDF 里会出现「图片」占位框）；可选「导出时撕掉胶带」做看答案版；逐页进度回调；PNG 固定 2×。合成顺序有测试兜底：荧光笔先画（压字不糊）、胶带画在它盖住的内容之后、像素橡皮的切段在数据层就完成 |

### 第三批交付（手写可搜 / 笔记本进 git / 图片裁剪 / 手写笔双击）

| 能力 | 落点 | 说明 |
| --- | --- | --- |
| **手写 OCR 搜索** | `docs/js/notebook/ocr.mjs` + viewer 的「识别手写文字」面板 | 页面渲染成 JPEG → SiliconFlow **Qwen3-VL**（8B 快 / 32B 准，OpenAI 兼容接口）→ 文字写回 `page.ocr` → 进资料库搜索、笔记本内搜索（会标「手写识别」来源）、`searchAll` 跨本命中。密钥只存本机 localStorage；默认只识别「没识别过且非空白」的页；并发 3 线程；错误按 401/402/429/5xx 给不同中文提示；还有「识别结果转成文本框」（手写 → 可编辑文本） |
| **笔记本同步到仓库** | `docs/js/notebook/sync.mjs` + 资料库「⋯ → 全部同步到仓库 / 从仓库拉取」+ 笔记本「更多 → 同步到仓库 / 从仓库拉取这本」 | 落点 `content/notebooks/<id>.json`（真身）、`docs/notebooks/<id>.json`（线上副本）、`docs/notebooks/index.json`（目录）。冲突规则：远端更新→拉取（覆盖前把本地存成「本机备份」副本）、本地更新→推送、时间相近→不动；另有**免令牌**的「线上笔记本」只读列表与一键导入 |
| **图片裁剪** | `page.mjs` 的 `beginCrop/applyCrop` + 套索面板「裁剪图片」 | 画布上八向控制点 + 三分线，确认后用离屏 canvas 裁出新图回写（可撤销）；换算逻辑 `cropPixels/dragCrop/cropHandles` 单独测 |
| **手写笔双击切工具** | `page.mjs` 的 `isPenDoubleTap` + `onPenDoubleTap`，设置入口在「更多 → 手写笔双击」 | 340ms / 12px 内的手写笔双击可切橡皮/画笔/荧光笔/轮换工具，双击那一笔不会落墨；**说明**：真正的 Apple Pencil 双击 API 浏览器不暴露，这是等效手势 |

---

## 1. 一句话结论

**Goodnotes 是「手写优先的笔记本 App」，本站是「Markdown 优先的静态笔记站」；本期按本站的取舍把前者的 6 层能力补齐了大部分，唯独第 ⑥ 层因结构性限制仍只能给替代方案。**

| 层 | 名称 | Goodnotes 的重心 | 本期交付后 |
| --- | --- | --- | --- |
| ① | 资料库层 | 文件夹 / 封面 / 模板 / 多选 / 回收站 | ✅ 基本齐了（11 项中 8 项完整） |
| ② | 笔记本 / 页面层 | 多页 + 缩略图 + 书签 + 纸张 | ✅ 齐了（仅动态模板与"真无限"白板受限） |
| ③ | 笔迹与工具层 | 笔 / 荧光笔 / 橡皮 / 套索 / 形状 / 尺子 / 胶带 / 激光笔 | ✅ 齐了（16 项中 12 项完整，含 4 笔型与压感） |
| ④ | 对象编辑层 | 位移缩放改色 / 层级 / 复制粘贴 / 撤销 | ✅ 齐了（仅密码保护本质不可行） |
| ⑤ | 学习与输出层 | 录音同步 / 手写搜索 / 闪卡 / 导出 | ✅ 齐了（录音同步、闪卡、PDF 导出均为真实现） |
| ⑥ | 云 / 协作 / AI 层 | iCloud + 实时协作 + AI | ❌ 结构性不可行，但**替代方案已交付**（本地摘要 / 备份 JSON / 只读分享） |

**结论**：79 项中 **58 项完整复现、11 项简化复现、6 项替代方案、4 项不可行**。第 ⑥ 层的 AI / 协作 / 云同步受「无后端、无 OCR 引擎、无大模型 API、纯静态托管」限制不可复现，但已交付可用的替代路径，下文逐条写明。

**一句话的分界线**：本站的护城河仍是「**文本可版本控制、可 diff、可 grep**」；本期把 Goodnotes 的「**手写即数据、可被检索与学习化**」补进来之后，两边的差距只剩"模型驱动的那部分"。

---

## 2. 能力拆解表

状态口径：✅ 完整复现 / 🟡 简化复现（写清缺什么）/ 🟠 替代方案（换一条路，**括号内注明本期是否已交付**）/ ❌ 不可行（结构性原因 + 替代方案）。

### ① 资料库层

| 功能 | 它是什么（一句话） | 本站复现方式 | 状态 |
| --- | --- | --- | --- |
| 资料库 Library | App 启动后所有笔记本的总入口 | `#/books` 视图（`library.mjs` + `notebook-library.css`）：顶栏统计 + 搜索 + 排序 + 网格/列表 + 多选 + 新建；导航含全部/收藏/回收站/文件夹/标签。原有 Markdown 资料库（`app.js` 书架）仍在 | ✅ |
| 多色文件夹 Multi-colored folders | 用颜色给文件夹分类归档，可加图标/emoji | 文件夹可新建/改名/删除/移入移出（`store.addFolder / renameFolder / removeFolder / moveToFolder`）；删文件夹只解绑不删内容。**缺**：文件夹颜色与 emoji 自定义 | 🟡 |
| 封面 Cover | 每本笔记本自定义封面颜色与图案 | 封面 12 色 × 6 花纹 + 标题首字标记（`paper.mjs` 的 `COVER_COLORS / COVER_PATTERNS / renderCover`）；新建与改封面两个入口都能选 | ✅ |
| 网格 / 列表视图 | 资料库两种排布切换 | 网格 / 列表一键切换，偏好存 localStorage（`note-books-lib`） | ✅ |
| 排序 Sort | 按更新时间 / 名称 / 日期排序 | 三种排序：最近打开 / 创建时间 / 标题（中文按 `zh-Hans-CN` 排）；收藏恒排前 | ✅ |
| 搜索 Search | 跨全部笔记本搜索标题与内容 | **首页全局搜索已合并笔记本**（`app.js` 的 `runSearch`）：Markdown 结果 + 「笔记本（含手写识别）」一段（按本聚合、命中处数、关键词高亮、点击跳回）；资料库搜索覆盖**标题 / 标签 / 文字摘要 / 已 OCR 的手写**；笔记本内可搜并标出来源（`searchText` / `searchAll`）。**前提**：手写要先在笔记本里跑一次 OCR | ✅ |
| 手写搜索（OCR） | 全书搜索手写内容，课上也能立刻查到 | **本期已交付**（`docs/js/notebook/ocr.mjs`）：把页面渲染成 JPEG → 交给 SiliconFlow 的 **Qwen3-VL**（8B 快 / 32B 准）识别 → 写回 `page.ocr` → 进入资料库与本笔记本搜索。三层保护：密钥只存本机 localStorage、默认只识别「没识别过的且非空白」的页、并发 3 线程 + 进度与错误中文提示（余额不足 / 限流 / 密钥无效分别给可操作方案）。另有「识别结果转成文本框」把手写变成可编辑文本 | ✅（需自备 API Key） |
| 多选批量操作 Multi-select | 长按进入多选，批量移动/删除/打标签 | 多选模式 + 底部批量条：移入文件夹 / 加标签 / 收藏 / 复制 / 移到回收站（`library.mjs` 选态 + `sel-*` 动作） | ✅ |
| 垃圾桶 Trash | 删除进垃圾桶，30 天内可恢复 | 回收站视图：恢复 / 彻底删除 / 清空 + 30 天自动清理提示（`TRASH_RETENTION_DAYS = 30`、`sweepTrash`） | ✅ |
| 收藏 / 置顶 Favorites | 标记常用笔记本置顶 | 封面星标一键收藏（卡片与列表行都有），收藏恒排前 | ✅ |
| 速览 Quick look | 不打开笔记本就能预览 | Markdown 侧速览浮层仍在（摘要 / 目录 / 出链入链）；笔记本侧靠卡片副标题（文字摘要）+ 缩略图 | ✅ |
| 新建笔记 / 模板选择 | 新建时挑一个预设模板 | 新建 sheet：标题 + 封面颜色 + 封面花纹 + 纸张模板 + 纸张颜色 + 尺寸；Markdown 侧仍有 4 套 frontmatter 模板。**缺**：Goodnotes 式"可导入的模板本" | 🟡 |

### ② 笔记本 / 页面层

| 功能 | 它是什么（一句话） | 本站复现方式 | 状态 |
| --- | --- | --- | --- |
| 多页笔记本 Page management | 增删复制排序页面、页面缩略图、书签 | 页面栈 + 加页/复制/删除/上移下移/拖拽排序 + 单页纸张覆盖（`viewer.mjs` 缩略图菜单 → `store.addPage / duplicatePage / removePage / movePage / setPagePaper`） | ✅ |
| 页面缩略图 Thumbnails | 侧栏一览全书页面，拖动调整顺序 | 左侧缩略图栏（真实渲染的页面小图），跳页 + 单页菜单 + 加页；手机上收成抽屉 | ✅ |
| 书签 Bookmark | 给关键页打书签，快速跳回 | 缩略图与页面面板都能设/取消书签，`outline()` 汇总为「大纲与书签」 | ✅ |
| 纸张模板 Paper templates | 空白 / 横线 / 康奈尔 / 清单 / 计划表 | **14 种**：空白 / 横线 / 方格 / 点阵 / 细格 / 康奈尔 / 清单 / 五线谱 / 分镜 / 周计划 / 月计划 / 年计划 / 打卡表 / 白板（`PAPER_TEMPLATES`，按基础/学习/创作/计划分组） | ✅ |
| 自定义纸张尺寸与颜色 | 自定义纸张长宽比与线条颜色，做方形本 / 蓝线本 | 8 种尺寸（含自定义宽高）+ 纸张颜色（含深色纸）+ `autoLineColor()` 按纸色自动给线色；单页可覆盖笔记本纸张 | ✅ |
| 动态模板 Dynamic templates | 模板内容随日期/输入自动变化 | 构建期渲染无法做运行时动态模板；**替代已交付**：纸张模板 + 打卡表/月计划等静态骨架，日期靠 frontmatter `date`（缺省取文件修改时间） | ❌（无服务端渲染）；替代已交付 |
| 无限白板 Whiteboard | 无限画布，自由头脑风暴 / 思维导图 / 画图表 | **替代已交付**：`白板` 纸张模板 + 视口缩放平移 + 形状/胶带/贴纸/文本框拼图；页面宽高仍固定，坐标按 0~1 比例存（换设备与打印对齐的前提） | 🟠 |
| 纸张方向 / 横竖翻页 | 笔记本纵向滚动或横向翻页 | 一键切换竖排滚动 ↔ 横排翻页（`scroll: 'vertical' \| 'horizontal'`，主题与渲染同步适配） | ✅ |

### ③ 笔迹与工具层

| 功能 | 它是什么（一句话） | 本站复现方式 | 状态 |
| --- | --- | --- | --- |
| 笔型 Pen types | 圆珠笔 / 钢笔 / 毛笔 / 铅笔 | **4 种笔型**（`PEN_TYPES`）：圆珠笔 / 钢笔 / 毛笔 / 铅笔，各自有独立的不透明度、基准笔宽、收缩区间与速度响应曲线，另有起收笔笔锋（`taper`） | ✅ |
| 压感与速度变宽 Pressure / velocity | 笔迹随下笔力度和速度变粗细 | 采集 `PointerEvent.pressure` 逐点存 `pressures[]`，无压感时（鼠标/普通触屏）自动回退到速度模型（`strokeWidths` 优先压感、否则 `velocityFactor`）；`normalizePageItems` 原样保留笔型与笔宽 | ✅ |
| 荧光笔 Highlighter | 半透明勾画，可自动拉直、不遮挡文字 | 5 色荧光笔 + 够直自动拉直（`straightenHighlighter`），半透明走 `multiply` 保证不遮字 | ✅ |
| 橡皮 Eraser | 整笔擦 / 像素擦 / 仅擦荧光笔 | **3 模式**（`ERASER_MODES`）：整笔 / 像素（长线切成两段）/ **仅荧光笔**；橡皮也能撕掉胶带（计入撤销） | ✅ |
| 套索 Lasso | 圈选对象，移动 / 缩放 / 旋转 / 改样式 / 复制 | **自由圈选 + 矩形选择**两种；移动 / 角点缩放 / 旋转柄旋转 / 改色 / **改透明度** / **置顶置底** / 复制粘贴，全部可撤销 | ✅ |
| 套索转文本 Lasso → text | 把圈中的手写转成可编辑文本 | 无 OCR，**替代已交付**：反向路径——用文本框打字，再被套索当对象统一移动/缩放/改样式；闪卡也能从文本对象自动生成 | 🟠 |
| 形状工具 Shapes | 直线 / 箭头 / 矩形 / 椭圆 / 三角 + 自动识别 + 填充 | 自动识别 + 下拉手选五种形状 + **可填充**（识别失败不硬套，乱涂保持原样） | ✅ |
| 文本框 Text box | 页面上任意位置打字，调字体字号颜色对齐 | 原位编辑、多行、拖动缩放；**4 字体**（黑体/宋体/手写/等宽）× 5 字号（极小→特大）+ **粗体/斜体/对齐** + 颜色 | ✅ |
| 尺子 Ruler | 屏幕上的直尺，沿尺画直线 | 尺子可拖动、可旋转（`rotateRuler`），笔画按 `snapToRuler` 吸附到尺边成直线，尺子本身按页面角点绘出 | ✅ |
| 胶带 Tape | 贴一条胶带把内容临时遮住 | 6 色和纸胶带 + 撕胶带（`tapeGeometry` 沿书写方向铺带；橡皮亦可撕；`peeled` 态）。**缺**：撕掉是删除（可撤销），不能"重新贴上/原地还原" | 🟡 |
| 激光笔 Laser Pointer | 演示时用激光点引导观众视线 | 演示模式内置激光笔：拖出带拖尾的红色激光轨迹，1.4 秒渐隐（`startLaser` + rAF 衰减） | ✅ |
| 页面缩放平移 Zoom / pan | 双指缩放平移页面 | 视口缩放 + 横竖滚动 + 演示态翻页；坐标按比例存，缩放后笔迹仍与正文对齐 | ✅ |
| 缩放窗 Zoom window | 屏幕角落的放大镜，写大写小 | 底部放大条：把页面一段区域放大绘制，可左右移动区域、可关闭；**写到右边沿自动往右推进**（`rel > 0.72` 触发，对齐 GoodNotes 的自动推进） | ✅ |
| 撤销 / 重做 Undo / Redo | 不限次撤销，含手势 | **跨页撤销重做**（笔记本内多页共享历史），覆盖画笔/荧光笔/橡皮/撕胶带/套索变换/改色改透明度/置顶置底/文本/图片/贴纸/胶带；每步带名字 | ✅ |
| 键盘快捷键 Shortcuts | 工具切换与编辑的快捷键 | 工具键（P/H/E/L/S/T 等）+ `[` `]` 调笔粗 + **Ctrl/⌘ + `[` / `]` 置底/置顶** + Delete/Esc；Markdown 标注层另有完整键位 | ✅ |
| Apple Pencil 手势 Pencil gestures | 涂黑擦除、画圈选择、双击切换工具 | 画圈选择成立（自由套索）；**双击切换工具本期已交付**——`pointerType === 'pen'` 的快速双击（`isPenDoubleTap`：340ms + 12px 内）可切成橡皮/画笔/荧光笔/轮换，且双击那一笔不会落墨；**缺**：真正的 Apple Pencil 双击 API 浏览器不暴露（iOS 专有），涂黑即擦除也没做 | 🟡 |

### ④ 对象编辑层

| 功能 | 它是什么（一句话） | 本站复现方式 | 状态 |
| --- | --- | --- | --- |
| 手写整体位移 / 缩放 / 改色 | 把手写当成对象：挪位置、缩放、换颜色 | 套索选中 → 拖动 / 角点缩放 / 旋转 / 改色 / 改粗细 / 改透明度（仿射变换有独立断言） | ✅ |
| 与打字同页混排 | 手写、打字、图片、贴纸同页共存 | 一页里笔迹 / 形状 / 文本 / 图片 / 贴纸 / 胶带 / 激光同层（`normalizePageItems` 统一规范），Markdown 正文与标注层仍可叠加 | ✅ |
| 对象复制粘贴 Duplicate | 复制、剪切、粘贴、原地再复制一份 | 套索复制粘贴 + Markdown 标注层的 Ctrl+C/X/V/D（原地偏移复制） | ✅ |
| 对象层级 Layer order | 调整对象前后遮挡顺序 | 置顶 / 置底（`zOrder('front'\|'back')` + Ctrl/⌘ `[` `]`）。**缺**：逐级前移/后移 | 🟡 |
| 贴纸 / 图章 / 和纸胶带 Stickers / Stamps / Washi tape | 从素材库拖贴纸、图章、和纸胶带做手账 | 贴纸元素库 4 组（标记/学习/生活/手账，每组 12 个）+ 6 色和纸胶带 + 撕胶带；贴纸可缩放旋转。**缺**：在线素材市场（本站不联网取素材） | ✅ |
| 插入图片 / 照片 Images | 页面上放图片与照片 | 插入图片对象（可移动/缩放/旋转）；Markdown 侧仍可上传到 `content/assets/` | ✅ |
| 图片裁剪 / 旋转 Crop / rotate | 对插入的图片裁剪旋转 | **本期已交付**：旋转（对象 `rot`）+ **裁剪**（`page.mjs` 里 `beginCrop/applyCrop`：画布上八向控制点 + 三分线，确认后离屏 canvas 裁出新的 data URL 回写，可撤销）；纯几何换算 `cropPixels` / `dragCrop` / `cropHandles` 都单独测过 | ✅ |
| 内部链接 Internal links | 页面 / 笔记之间互相跳转 | `[[双链]]`（出链入链 + 知识图谱可拖节点）+ 页内锚点 + 笔记本内页码/大纲跳转 | ✅ |
| 密码保护笔记本 | 给单本笔记加密码 | **不可行**（静态站内容公开可抓，前端密码等于摆设）；替代：敏感内容不进仓库 | ❌（静态托管无访问控制） |

### ⑤ 学习与输出层

| 功能 | 它是什么（一句话） | 本站复现方式 | 状态 |
| --- | --- | --- | --- |
| 录音与手写时间点同步 Audio synced to writing | 录课时把每句话锚定到当时写下的笔迹 | MediaRecorder 录音 + `store.audioAnchor()` 记录"录到这段时正在写的那一页/那一笔"；**播放时按进度自动翻回对应页**（`viewer.mjs` 录音面板） | ✅ |
| 音频转写 Transcription | 把录音自动转文字（会议纪要） | **第八批已交付**：`asr.mjs` 走 SiliconFlow `/v1/audio/transcriptions`（多语种 / 中文两档模型），转写挂在录音记录上，笔记本内搜索 / 资料库搜索 / 首页搜索都能命中；**边界**：需自备 Key、音频会上传第三方、暂不做说话人分离与时间戳对齐 | 🟡 |
| Study Sets 闪卡（主动回忆 + 间隔重复） | 做闪卡并按时复习 | 学习集面板：建卡/删卡/正面翻面/"记住了·忘了"→ **Leitner 盒 1/2/4/8/16/32 天**、`dueCards()` 今日到期、`deckStats()` 掌握度；`cardsFromText()` 从页面文本对象按「前 —— 后」自动生成卡片 | ✅ |
| 交互式考试练习 Interactive Exam Practice | 应用内找真题（SAT/DSE 数学）并在里面做 | **不可行**（无题库运营与判题服务）；替代：真题写成任务清单笔记 + 页面当草稿纸 | ❌（无题库与判题服务） |
| 手写搜索（OCR） | 全书搜索手写内容，课上也能立刻查到 | **本期已交付**（`docs/js/notebook/ocr.mjs`）：页面渲染成 JPEG → SiliconFlow **Qwen3-VL**（8B 快 / 32B 准）→ 文字写回 `page.ocr` → 资料库与本笔记本搜索都能命中。密钥只存本机、默认只跑没识别过的页、并发 3 + 中文错误提示 | ✅（需自备 API Key） |
| PDF 标注 PDF annotation | 在任何 PDF 上手写批注 | PDF 放 `content/assets/` 或当图片插入，手写层叠在其上批注 | ✅ |
| PDF 目录自动导入 | 打开 PDF 自动读取它的目录当大纲 | `tools/extract-attachments.mjs` 内置极简 PDF 解析器（FlateDecode + 抽文本算子）能抽正文文本进检索；**缺**：书签树（目录）未解析，扫描件需 OCR | 🟡 |
| 自定义大纲 Outline | 自己给长文档建大纲 | 笔记本"大纲与书签"面板（书签页 + 有标题的页）；Markdown 侧有「## 目录」卡片式索引 + 右侧随滚动高亮的本页目录 | ✅ |
| 导出 PDF / 打印 | 导出手写+打字内容为 PDF，或直接打印 | **自写 PDF 写出器**（`study.pdfFromImages`）：逐页渲染成 JPEG，按 `/DCTDecode` 嵌入，含 xref / startxref，字节流有断言（`%PDF`…`%%EOF`、对象偏移与页面数正确）；另有"打印"（只输出页面）；Markdown 侧可 Ctrl/Cmd+P | ✅ |
| 分享到任意位置 Export / share | 导到邮件、打印、分享 PDF | 导出 PDF / 当前页 PNG / 本本 JSON / 打印 / 备份 JSON 下载；对外分享靠 GitHub Pages 链接与离线便携版 HTML。**缺**：邮件直发（无后端） | ✅ |
| 实时分享链接（协作编辑同一份笔记） | 发个链接，多人同时改同一本笔记 | **不可行**（无后端）；**替代已交付**：Pages 只读链接 + 仓库协作（PR/commit）+ 编辑器"远端被改过先提示" | 🟠 |
| AirPlay / HDMI 演示 Present to external screen | 把笔记投到外接屏幕演示 | **演示模式已交付**：全屏、只留页面与激光笔、方向键或滑动翻页、Esc 退出、隐藏提示条；投屏用系统扩展屏幕/投屏即可 | ✅ |
| 邮件导入文档 Import by email | 把文档发到专属邮箱自动进资料库 | **不可行**（无收信服务）；**替代已交付**：文件丢进 `content/` 或 `content/assets/` 后 `node tools/update.mjs` 一条命令完成提取→构建→自检→提交→推送 | 🟡 |
| 云备份（Google Drive / Dropbox / OneDrive） | 全量笔记备份到第三方云盘 | **不可行**（无 OAuth 中继与后台任务）；**替代已交付且更实用**：`sync.mjs` 把笔记本推进 git 仓库（`content/notebooks/<id>.json` 真身 + `docs/notebooks/` 线上副本 + `docs/notebooks/index.json` 目录），于是有完整版本历史、可 diff、可回滚；另有「导出备份 JSON / 导入备份」（同 id 存副本不覆盖） | 🟠（替代更强） |
| iCloud 跨设备同步 | iPad / iPhone / Mac 之间自动同步 | **不可行**（无后端）；**替代已交付**：同一 GitHub 仓库 + Pages 同一网址；`sync.mjs` 的推送/拉取带冲突判定（远端更新→覆盖前先存本机备份副本，本地更新→推送，时间相同→不动，避免来回横跳），另有免令牌的「线上笔记本」只读导入 | 🟠（替代更强） |
| 多人实时协作 Collaboration | 团队在同一白板上同步书写 | **不可行**（无 WebSocket 服务）；**替代已交付**：仓库协作者各自 commit，冲突交 git + 编辑器保存前冲突检测 | 🟠 |
| 会议纪要 Meeting Notes | 录音转写后自动生成纪要 | **替代已交付**：录音 + 时间锚定 + 会议复盘模板（frontmatter + `> [!TODO]` + `- [x]` 清单）+ 全文检索回查 | 🟡 |

### ⑥ 云 / 协作 / AI 层

| 功能 | 它是什么（一句话） | 本站复现方式 | 状态 |
| --- | --- | --- | --- |
| Goodnotes AI 总结 Summary | 把一周日记 / 一页笔记总结成要点 | **不可行**（无大模型 API）；**替代已交付**：`study.summarizeText / summarizePage` 本地抽取式摘要（按词频取关键句、含术语优先、保持原文顺序），测试断言"取到关键句而非闲聊" | 🟠 |
| AI 缩短 / 扩写 / 改语气 Shorten / expand / change tone | 对打字文本做润色三连 | **不可行**（无大模型）；替代：存"改前/改后"两版，靠 git diff 对比语气。可选的旁路是把用户自己的 Key 填在本地后调一次、把结果当文本插入笔记 | ❌（无大模型） |
| AI 转写 Transcript support | 把录音与会议转成可读文本 | **第八批已交付**：与「音频转写」同一套（ASR 出文本 → 进搜索/可导出 Markdown）；**边界**：只转写不做润色与结构化纪要（那需要大模型） | 🟡 |
| AI 出题 / 学习材料生成 | 把乱糟糟的手写笔记变成结构化复习材料 | **不可行**（无大模型）；**替代已交付**：`cardsFromText()` 规则式从文本生成闪卡（机械规则，非 AI） | 🟠 |
| AI 手写学习（在你自己的字体里纠错） | 学习用户手写字形，在保留笔迹字体的情况下替换错词 | **不可行**：需端侧专有 ML 模型 + 手写样本训练（官方注明模型 proprietary、全在设备上运行） | ❌（无端侧 ML） |
| 拼写检查（手写内）Spellcheck | 手写内容里的拼写错误自动纠正 | **不可行**（无手写字形模型）；替代：文本框与 Markdown 走浏览器原生 spellcheck | ❌（无手写字形模型） |
| 词补全 Word Complete | 手写场景下的"自动补全"（官方已于 2025-03 停用） | **官方已下线**，不列为复现目标；文本框用输入法自带联想 | ❌（官方停用） |
| 手写算式识别 / Math Assist | 识别手写算式并给出正确性提示、支持公式转换 | **不可行**（无 OCR/公式模型）；**替代已交付**：编辑器 `∑ ∫` 按钮插 `$...$` / `$$...$$`，KaTeX 渲染（CDN 不可用时降级等宽文本） | 🟠 |
| Scribble 手写转文字 | 手写直接变系统文本 | **本期已交付等效路径**：先用 Qwen3-VL 识别这一页（`ocr.mjs`），再点「识别结果转成文本框」把手写落成可编辑文本对象（可拖动/改样式/可撤销）。**缺**：不是系统级实时转换，需要手动点一次 | 🟡 |
| Ask Goodnotes（对话式提问） | 对笔记内容提问，AI 回答 | **不可行**（无大模型）；替代：知识图谱 + 双链 + 全文检索（先靠结构找到内容，再靠自己回答） | ❌（无大模型） |

### 平台 / 系统集成

| 功能 | 它是什么（一句话） | 本站复现方式 | 状态 |
| --- | --- | --- | --- |
| 多窗口 / 分屏 Multi-window | 两本笔记并排对照 | 浏览器开两个标签/窗口（hash 路由 `#/book/<id>` 直接定位）；**缺**：应用内分屏 | 🟡 |
| 日历集成 Calendar | 计划本与系统日历联动 | 时间线视图按月份倒序 + frontmatter `date` + 周/月/年计划纸张模板；**缺**：与系统日历双向同步（需原生壳） | 🟡 |
| Widget 桌面小组件 | 主屏小组件显示最近笔记 | **替代已交付**：PWA「添加到主屏幕」（`manifest.webmanifest` + `sw.js` 离线）+ 三种桌面快捷方式；**缺**：原生小组件 | 🟠 |
| PWA / 离线 | 断网可用 | Service Worker 离线缓存 + 「添加到主屏幕」+ `tools/build-portable.mjs` 单文件离线便携版 | ✅ |
| 离线便携版单文件 | 拷一个文件到别的机器就能看 | `dist/我的笔记-离线版.html`，断网双击即开（KaTeX 降级为等宽文本，内容不丢） | ✅ |
| 计划本 / 年-月-周模板 Planners | 内置与可导入的年 / 月 / 周计划模板 | 周计划 / 月计划 / 年计划 / 打卡表 四套纸张模板 + 时间线视图 + `- [x]` 清单 | ✅ |
| 日记 Daily journal | 手写 + 打字感想 + 照片 + 贴纸同页 | 同一页里手写笔迹 + 文本框 + 图片 + 贴纸 + 和纸胶带，日期用 `date`；Markdown 侧插照片 | ✅ |
| 键盘 / 触控笔支持 | 外接键盘与手写笔输入 | Pointer Events 统一处理鼠标/触屏/手写笔，`pointerType` 区分手势，`pressure` 驱动笔宽；外接键盘用全套快捷键 | ✅ |

---

## 3. 分层复现蓝图

### ① 资料库层 — ✅ 本期已交付

| 项 | 状态 |
| --- | --- |
| `docs/js/notebook/library.mjs` + `notebook-library.css`：顶栏统计/搜索/排序/网格列表/多选批量/文件夹/标签/收藏/回收站/新建 sheet/备份导入导出/空状态 | **已完成** |
| 数据层 `store.mjs`：文件夹与笔记本 CRUD、`notebooks({q,folder,fav,trash,sort,tags})`、`stats()`、回收站 30 天、`exportJSON/importJSON`、老数据 `migrate` | **已完成** |
| 路由接线：`app.js` 的 `#/books` 与资料库视图 host、`index.html` 的 `#view-books` | **已完成** |
| 后续可做：文件夹自定义颜色与 emoji；资料库内统一检索两种数据源（笔记本 + Markdown 正文）；封面花纹再扩几种 | 后续 |

### ② 笔记本 / 页面层 — ✅ 本期已交付

| 项 | 状态 |
| --- | --- |
| `paper.mjs`：14 种纸张模板 + 8 种尺寸（含自定义）+ 纸张颜色与 `autoLineColor` + 封面 12 色 × 6 花纹 | **已完成** |
| `viewer.mjs`：页面栈、缩略图栏、页面增删复制排序书签、大纲、纸张设置、横竖翻页切换 | **已完成** |
| `store.mjs`：`addPage / duplicatePage / removePage / movePage / setPagePaper / clearPagePaper / toggleBookmark / setPageTitle / outline` | **已完成** |
| 后续可做：把 Markdown 笔记一键转成笔记本；页面模板批量套用 | 后续 |
| 不做：动态模板（需运行时渲染）；真"无限"画布（页面宽高固定是"换设备与打印对齐"的前提，用白板模板 + 视口缩放替代） | 不做 |

### ③ 笔迹与工具层 — ✅ 本期已交付

| 项 | 状态 |
| --- | --- |
| `page.mjs` 引擎：`PEN_TYPES` 4 笔型 + 速度/压感笔宽 + 笔锋；荧光笔自动拉直；`ERASER_MODES` 3 模式；套索自由/矩形 + 移动缩放旋转 + 改色改透明度 + 置顶置底；形状识别 + 手选 + 填充；文本框 4 字体 × 5 字号 + 粗斜对齐；图片；`ELEMENTS` 贴纸库；和纸胶带 + 撕胶带；激光笔；尺子吸附；放大窗 + 自动推进；视口缩放；跨页撤销重做 | **已完成** |
| `normalizePageItems` 保留笔型/笔宽/透明度/字体，避免撤销一次就把"毛笔""自定义笔宽"抹掉 | **已完成** |
| 后续可做：胶带"重新贴上/原地还原"（现在是撕掉即删除 + 撤销）；图片裁剪；逐级前移/后移；压感曲线可调档 | 后续 |

### ④ 对象编辑层 — ✅ 本期已交付

| 项 | 状态 |
| --- | --- |
| 七类对象统一规范 + 撤销走同一条"操作前快照"路径（`page.mjs` 与 `ink.mjs` 两套引擎同一套语义） | **已完成** |
| 层级置顶/置底 + 快捷键；透明度；复制粘贴 | **已完成** |
| 后续可做：图片裁剪、对齐吸附辅助线、对象分组 | 后续 |
| 不做：密码保护（静态站内容公开可抓，前端密码无意义） | 不做 |

### ⑤ 学习与输出层 — ✅ 本期已交付

| 项 | 状态 |
| --- | --- |
| `study.mjs`：自写 PDF 写出器（JPEG `/DCTDecode` + xref/startxref）、PNG 导出、打印、MediaRecorder 封装、本地抽取式摘要 | **已完成** |
| 闪卡 + Leitner 间隔重复 + `cardsFromText` 自动生成 + `deckStats/deckQueue` | **已完成** |
| 录音 + `audioAnchor` 时间锚定 + 播放自动翻页 | **已完成** |
| 演示模式（全屏 + 激光笔 + 方向键/滑动翻页） | **已完成** |
| 后续可做：手写搜索接 `tesseract.js`（可选、按需加载、`chi_sim`）把结果写进 `content/ink/<slug>.ocr.json` 供检索；PDF 书签树（目录）解析 | 后续 |
| 不做：音频转写、题库与判题、邮件收信（均需后端或模型） | 不做 |

### ⑥ 云 / 协作 / AI 层 — 结构性不可行，替代方案已交付

| 项 | 状态 |
| --- | --- |
| 备份：`exportJSON` 下载 + 导入备份（同 id 按新副本并入，不覆盖）+ git 仓库本身即异地备份 | **已完成（替代）** |
| 本地摘要：`summarizeText / summarizePage` 抽取式摘要，替代"AI 总结" | **已完成（替代）** |
| 分享：Pages 只读链接 `#/book/<id>` + 单文件离线便携版 HTML | **已完成（替代）** |
| 一个资料库 = 一份 JSON（`{version, folders, notebooks[], settings}`）+ 老数据 `migrate` | **已完成** |
| 不做：实时协作（需 WebSocket 服务与冲突合并）、iCloud 式端侧同步（需原生壳与账号体系）、手写字形学习与手写内拼写检查（需端侧专有 ML + 手写样本）、云端 OCR/公式识别、大模型出题与润色（无 API 与密钥） | 不做（原因见下节） |
| 可选旁路：用户自己在本地填 API Key（与现有 GitHub Token 同策略，只存本机）→ 对选中文本调一次 → 结果**作为文本插入笔记**（天然进 git、可 diff、可撤销）；无 Key 时降级为纯本地 | 后续（低优先级） |

---

## 4. 不可行清单与诚实说明

三条硬约束，所有 ❌ 都从这里派生：

1. **无后端**（纯静态托管在 GitHub Pages，只有 `api.github.com` 一条写路径，且靠用户自己的 Token）
2. **无 OCR / 无语音引擎**（零依赖设计，构建脚本只用 Node 内置模块，不装 `node_modules`）
3. **无大模型 API、无端侧 ML**（既无密钥也无推理运行时）

| # | 不可行项 | 为什么不可行 | 替代方案（本站口径） |
| --- | --- | --- | --- |
| 1 | 多人实时协作 | 无 WebSocket / 无状态服务，静态站无法广播他人光标与笔迹；轮询会造成"两份都改了"的静默覆盖 | 仓库协作（各自 commit，冲突交 git）+ 编辑器"远端被改过先提示"；分享链接一律只读 |
| 2 | 手写搜索 OCR | 零依赖环境下无可靠 OCR（README 明确：图片只能标 `pending-ocr`，需人工补文本） | **已交付**：文本对象可搜（`store.searchText`）+ 页面书签/标题大纲 + 标签筛选；后续可按 `extract-attachments.mjs` 注释接 `tesseract.js` |
| 3 | AI 总结 / 润色 / 出题 | 无模型 API 与密钥；把密钥写进静态前端等于公开泄漏 | **总结已交付替代**（本地抽取式摘要）；**出题已交付替代**（规则式从文本生成闪卡）；润色走"存两版 + git diff"或用户自带 Key 的旁路 |
| 4 | 音频转写 / 会议纪要自动生成 | 无语音模型 | 录音 + 时间锚定 + 会议复盘模板 + 全文检索回查；转写文本手工/外部补入 Markdown |
| 5 | 手写字形学习 / 手写内拼写检查 | 需端侧专有 ML 模型与用户手写样本训练（官方注明模型 proprietary、全设备内运行） | 文本框与 Markdown 用浏览器原生 spellcheck；中文输入法自带纠错 |
| 6 | Scribble / Math Assist / 手写公式识别 | 同上，需 OCR + 公式解析模型 | **已交付替代**：编辑器 `∑ ∫` 插 `$...$` / `$$...$$`，KaTeX 渲染（断网降级等宽文本，内容不丢） |
| 7 | 云同步（iCloud / Drive / Dropbox / OneDrive） | 无 OAuth 中继、无后台任务，纯前端拿不到这些云盘的长期凭据 | **已交付替代**：git 即异地备份（且带完整版本历史）+ 备份 JSON 导出/导入 + Pages 同一网址跨设备 |
| 8 | 邮件导入文档 | 需要收信服务器与解析队列 | **已交付替代**：文件丢进 `content/` 或 `content/assets/`，`node tools/update.mjs` 一条命令走完 12 步（任一步失败都不提交） |
| 9 | 密码保护笔记本 | 静态站内容全部公开可抓取，前端密码只遮住自己 | 敏感内容不进仓库；需要真保护就换带鉴权的托管 |
| 10 | 交互式考试练习（题库 + 判题） | 无题库运营、无判题服务 | 真题写成 `- [x]` 清单笔记，页面当草稿纸；对答案靠手工 |
| 11 | Widget / 系统日历双向同步 | 需要原生壳（WidgetKit / 系统日历 API） | PWA「添加到主屏幕」+ 桌面快捷方式；日历维度用时间线视图 + 周/月/年计划纸张模板 |
| 12 | 动态模板 | 需服务端或不重启的实时渲染；本站 Markdown 是**构建期**渲染（页面不闪的代价） | frontmatter 缺省值 + 静态模板骨架；真要动态就在编辑器里展开后落盘 |
| 13 | 真"无限"白板 | 页面宽高固定、坐标按 0~1 比例存（这是换设备与打印对齐的前提） | **已交付替代**：白板纸张模板 + 视口缩放平移 + 形状/胶带/贴纸/文本框拼图 |
| 14 | Word Complete | 官方（Goodnotes）已于 2025-03-31 停用该实验功能 | 不在复现范围；文本框用输入法自带联想 |

> **诚实说明**：上表 14 条中，第 1、3、4、5、6、7、8、11、12、14 条是**设计使然**——本站的核心取舍是"内容永远是 Markdown 文件、零依赖、构建期渲染"，换来的是十年后仍能跑、可 diff 可 grep、无需命令行也能改；第 2、9、10、13 条是产品边界（无 OCR、无鉴权、无题库、画布有限）。其中第 2、3、6、7、8、13 条的替代方案**本期已经交付**，不是待办。

---

## 5. 验收清单（手工回归用）

> 笔记本模式入口：站点 `#/books`（资料库）→ 点封面进 `#/book/<id>`。Markdown 标注层入口：笔记页 → `✍ 标注`。

### ① 资料库层

- [ ] 打开 `#/books` → 顶栏显示笔记本数与统计，网格出现封面（颜色 + 花纹 + 首字）→ ① 资料库
- [ ] 切换网格 / 列表 → 两种视图同一批笔记本；刷新后偏好仍在 → ① 视图
- [ ] 排序切「最近打开 / 创建时间 / 标题」→ 顺序随之变化，且收藏恒排前 → ① 排序
- [ ] 搜索框输入标题关键词 / 标签 → 命中收敛；输入手写内容里的词 → **预期搜不到**（记为差异，替代见 ⑤）→ ① 搜索边界
- [ ] 新建文件夹 → 把两本移入 → 导航点该文件夹只显示这两本；删文件夹 → 内容仍在、变"不归属" → ① 文件夹
- [ ] 点封面星标 → 进入「收藏」；取消 → 移出 → ① 收藏
- [ ] 多选（多选按钮 + 勾选）→ 底部批量条出现 → 批量加标签 / 收藏 / 移入文件夹 / 复制 / 移到回收站各生效一次 → ① 多选批量
- [ ] 移到回收站的两本 → 「回收站」可见并可恢复；「彻底删除」需二次确认；界面提示"30 天后自动清理" → ① 回收站
- [ ] 顶栏「导出备份」→ 下载 JSON；改几本后再「导入备份」→ 同 id 按新副本并入，**现有内容不被覆盖** → ① 备份
- [ ] 新建笔记本 sheet：改封面颜色 / 花纹 / 纸张模板 / 纸张颜色 / 尺寸 → 创建后封面与首页纸张与所选一致 → ① 新建

### ② 笔记本 / 页面层

- [ ] 打开一本 → 左侧缩略图栏显示各页真实小图；点缩略图跳页 → ② 缩略图
- [ ] 加页 / 复制页 / 删除页 / 上移下移 / 拖拽排序 → 页码与缩略图顺序同步更新 → ② 页面管理\n- [ ] 「更多 → 页面总览（批量管理）…」→ 一屏看到全部页 → 点几页选中（Shift 连选）→ 批量复制 / 删除 / 移到最前 → 回缩略图栏看顺序与页数都对了 → 双击总览里的某页能直接跳过去 → ② 页面总览
- [ ] 某页设书签 → 缩略图出现 🔖，打开「大纲」面板能跳到该页；取消书签即消失 → ② 书签
- [ ] 纸张设置里依次试 14 种模板（空白/横线/方格/点阵/细格/康奈尔/清单/五线谱/分镜/周/月/年/打卡/白板）→ 每种都画出对应底纹 → ② 纸张模板
- [ ] 纸张尺寸切自定义并改宽高 → 页面比例立即变化；换纸色 → 线色随之自动协调 → ② 自定义纸张
- [ ] 单页设置纸张（与笔记本纸张不同）→ 只有该页变，其它页不动；清除覆盖 → 回到笔记本纸张 → ② 单页纸张
- [ ] 切换竖排滚动 ↔ 横排翻页 → 布局与翻页方式都切换 → ② 纸张方向

### ③ 笔迹与工具层

- [ ] 依次选圆珠笔 / 钢笔 / 毛笔 / 铅笔各写一笔 → 四笔的粗细、透明度与收笔笔锋明显不同 → ③ 笔型
- [ ] 用鼠标快速划与慢速划 → 快速划更细（速度模型）；若有笔设备，用力与轻写 → 笔宽随压感变化 → ③ 压感/速度
- [ ] 🖍 荧光笔画一条直线 → 自动拉直；画在文字上 → 文字仍清晰可读 → ③ 荧光笔
- [ ] 橡皮「整笔」擦过一条线 → 整条消失；「像素」擦长线中段 → 断成两段；「仅荧光笔」擦 → 只掉荧光笔、墨迹还在 → ③ 橡皮三模式
- [ ] 橡皮擦过胶带 → 胶带被撕掉，且可撤销复原 → ③ 橡皮撕胶带
- [ ] 套索「自由圈选」圈两条笔画 → 拖动移动 / 角点缩放 / 圆点旋转 → ③ 套索自由
- [ ] 切「矩形选择」→ 框出区域选中内容 → ③ 套索矩形
- [ ] 选中对象后改颜色、改透明度、点「置顶」「置底」（或 Ctrl/⌘ `[` `]`）→ 样式与遮挡顺序都变，且可撤销 → ③ 套索改样式与层级
- [ ] 圈选几个对象 → 直接拖到左侧某一页的缩略图上（拖动时那个缩略图会高亮）→ 松手：对象出现在那一页；回原页看不到了 → Ctrl/⌘+Z **一次**：两页同时复原（源页对象回来、目标页不残留） → ③ 跨页拖拽
- [ ] ▭ 形状随手画圆 → 规整成椭圆；画三角 → 规整成三角；**故意乱涂 → 不硬套形状**；勾选填充 → 图形有填充色 → ③ 形状
- [ ] 🅣 文本框点一下输入 → 换 4 种字体、5 档字号、粗体/斜体/对齐 → 全部生效；双击已完成的文字 → 可再编辑 → ③ 文本框
- [ ] 打开尺子 → 拖动、旋转 → 沿尺写一笔 → 笔迹是吸附在尺边的直线 → ③ 尺子
- [ ] 贴一条和纸胶带盖住内容 → 换 6 种颜色 → 撕胶带 → 露出内容，且可撤销 → ③ 胶带
- [ ] 进入演示模式 → 点激光笔在页上拖动 → 红色拖尾 1.4 秒渐隐；方向键/滑动翻页；Esc 或提示条退出 → ③ 激光笔 + 演示
- [ ] 缩放页面 → 笔迹与正文仍对齐；切换横排后翻页正常 → ③ 视口缩放
- [ ] 打开放大窗 → 在条内写字 → 写到右边沿自动往右推进；左右按钮移动放大区域、✕ 关闭 → ③ 放大窗
- [ ] 连续做 10 个不同动作（含撕胶带、置顶、改透明度）后连按撤销 → 逐一回退且提示带动作名；重做可恢复 → ③ 撤销/重做
- [ ] 第 1 页编辑 → 翻到第 3 页编辑 → 回第 1 页撤销 → 撤销跨页生效且不串页 → ③ 跨页撤销
- [ ] 依次按工具快捷键（画笔/荧光笔/橡皮/套索/形状/文本）→ 工具切换；`[` `]` 调笔粗 → ③ 快捷键

### ④ 对象编辑层

- [ ] 同一页放齐笔迹 / 形状 / 文本 / 图片 / 贴纸 / 胶带 → 刷新后全部还在、相对位置不变 → ④ 同页混排
- [ ] 插入一张图片 → 可移动、缩放、旋转 → ④ 图片
- [ ] 贴纸元素库里挑几个贴到页面（标记/学习/生活/手账各一）→ 都能缩放旋转 → ④ 贴纸
- [ ] 选中对象复制 → 出现副本；Delete 删除 → 移开 → ④ 复制/删除
- [ ] 撤销一次后复查：笔型与自定义笔宽仍保留（没被"还原成圆珠笔"）→ ④ normalizePageItems
- [ ] Markdown 侧：双击已有文字可再编辑；文本与图片不会被橡皮误删 → ④ 对象保护
- [ ] 双链 `[[标题]]` → 出链入链与图谱节点可跳转 → ④ 内部链接

### ⑤ 学习与输出层

- [ ] 学习集面板建卡 → 翻面 → 「记住了」→ 到期时间变为 +1 天；连记 5 次 → 间隔 2/4/8/16/32 天；点「忘了」→ 盒归 0 → ⑤ 闪卡间隔重复
- [ ] 复习中心 →「来源整理…」→ 看两类计数 → 「自动找回」→ 唯一命中的卡挂上来源、找不回的被跳过 → 「清掉失效来源」只清来源页已删的 → ⑤ 来源整理\n- [ ] 在某一页建卡 → 卡的来源显示「来自《本名》第 N 页」→ 点「去看原页」跳到那一页 → 回资料库仍停在同一张卡（复习进度没丢）→ ⑤ 来源页
- [ ] 把某一页删掉 → 那张卡的来源显示「来源页已被删除」→ 点「清掉来源」后恢复正常 → ⑤ 来源坏掉的处理
- [ ] 从页面文本对象「前 —— 后」自动生成闪卡 → 张数与内容匹配 → ⑤ 出题（规则替代）
- [ ] 开始录音 → 在第 2 页写几笔 → 停止 → 播放该录音 → **自动翻到第 2 页**（时间锚定）→ ⑤ 录音同步
- [ ] 录音面板删除一段录音 → 列表与数据同步移除 → ⑤ 录音管理
- [ ] 导出 PDF → 文件以 `%PDF` 开头、`%%EOF` 结尾，页数与笔记本一致，手写与纸张都在；用系统阅读器能打开 → ⑤ 导出 PDF
- [ ] 「导出 ▾ → 排版：2 页拼一张（A4 横版）」→ 再导出 → 提示写「N 页 → M 面」→ 打开 PDF：每张纸上左右两页、纸是横版；**Ctrl+F 仍能搜到文字**、点「见第 3 页」仍跳得对 → ⑤ 省纸排版
- [ ] 「排版：骑马钉小册子」导出 8 页的本子 → 得到 4 面：第 1 面左 8 右 1、第 2 面左 2 右 7…（对折中缝装订后按顺序翻） → ⑤ 骑马钉
- [ ] 导出当前页 PNG → 图片内容与屏幕一致 → ⑤ 导出 PNG
- [ ] 打印 → 只输出页面内容（无工具栏），可"另存为 PDF" → ⑤ 打印
- [ ] 顶部摘要入口对一页文字生成摘要 → 输出为原文中的关键句且顺序不乱 → ⑤ 本地摘要（AI 总结替代）
- [ ] 大纲面板列出书签页与有标题的页，点击可跳转 → ⑤ 大纲
- [ ] 给某一页写个标题（标题文字写在页面中段）→ 导出 PDF → 阅读器侧栏点这个书签：**滚动到底部那一行**（不是页首）→ ⑤ 书签页内落点
- [ ] 「导出 ▾ → 打印检查单…」→ 看到「N 页 → M 面 · 双面打印约 K 张纸」与注意事项（空白页 / 只有胶带 / 没识别 / 内容密）→ 点警告里的页号能跳过去 → 开「跳过空白页」再导出：PDF 页数变少，且书签与目录页码没有指错 → ⑤ 打印检查单
- [ ] 用工具链：`node tools/extract-attachments.mjs` → `node tools/build.mjs` → 常规 PDF 正文被搜索命中（扫描件标 `pending-ocr`）→ ⑤ PDF 文本
- [ ] OCR 面板勾「同时识别行位置」跑一页手写 → 点「**把识别结果换成文本**」→ 手写在原位变成可编辑文本（贴纸/胶带还在）→ Ctrl+Z **一次**全退回手写 → ⑤ 手写转文字
- [ ] 资料库多选 2~3 本 → 「**识别手写**」→ 面板里选模型与并发、确认「本次要识别 N 页」→ 开始 → 进度条与失败数实时更新；中途点「停止」→ 已识别的页都在 → 回资料库顶部出现「手写识别队列还剩 N 页」→「继续识别」只跑剩下的（**用一次请求数就能验证：已识别的页不再发请求**）→ ⑤ 批量识别队列
- [ ] 资料库横幅「**复习中心**」→ 按某个标签筛（按钮上写着还剩几张）→ 「开始复习」→ 一张张过（翻面 → 记住了 / 忘了 / 跳过）→ 结束时给「记住 N / 忘了 M」；「忘了」的那张 10 分钟后回到队列 → ⑤ 筛着复习
- [ ] 复习几张后在复习中心看**热图**：今天那格变深、连续天数 +1（今天没复习时不该显示"断签"）→ ⑤ 复习热图
- [ ] 复习中心「导出 Anki CSV」→ 用文本编辑器看：前三行是 Anki 需要的头（`#separator:Comma` 等）、一行一张卡、标签列含 `本_笔记本名`；导进 Anki（逗号分隔 + 允许 HTML）能正常成卡 → ⑤ Anki 导出
- [ ] 故意填一个错的 API Key 跑队列 → 立刻停住并提示「密钥无效」，**不是**把几十页挨个失败一遍 → ⑤ 队列短路
- [ ] 把 Key 改对 → 面板「只重试失败的 N 页」→ 成功后再看：失败痕迹消失、这几页内容进搜索 → ⑤ 失败重试
- [ ] 跑完一批后点「识别质量清单…」→ 看有没有「字数异常少 / 疑似乱码 / 内容重复 / 缺少行位置」的页 → 点「去看看」跳到那一页核对 → 回清单点「重跑这些 N 页」→ 再跑一次清单，这几页应离开清单或理由变化 → ⑤ 质量回看闭环
- [ ] 清单里点「重跑并补行位置」→ 跑完导出 PDF，在那几页上框选复制，文字应贴着原位（行框补上了） → ⑤ 补行位置
- [ ] 资料库卡片「⋯ → 换封面图」传一张自己的图 → 封面按比例裁切显示；「移除封面图」退回配色封面；导出备份 JSON 后重导入 → 封面图仍在 → ⑤ 封面图
- [ ] 做几张到期闪卡 → 资料库顶部出现「今天该复习 N 张卡」→ 点「开始复习」→ 直接进那一本并弹出闪卡面板 → 复习完横幅消失 → ⑤ 复习入口
- [ ] 导出菜单「导出 EPUB」→ 得到 `.epub`（重命名 .zip 可看结构：`mimetype` 在第一条且不压缩、有 `cover.xhtml` / `toc.ncx`）→ 用阅读器打开、搜手写识别里的词能搜到 → ⑤ EPUB
- [ ] 同一本里给两页写标题、一页只设书签 → 再导出 EPUB → 阅读器的目录里那页书签**挂在标题页下面**（层级）；正文写「见第 3 页」→ 点一下真的跳到第 3 页 → ⑤ EPUB 层级目录与内链
- [ ] 资料库给这本换个自定义封面图 → 再导出 EPUB → 书封变成那张图（没传封面图时是按配色封面自动画的） → ⑤ EPUB 书封
- [ ] 导出菜单「导出单文件 HTML」→ 一个 `.html` 文件双击即开，图片全在（无外链）、打印时分页 → ⑤ 单文件 HTML
- [ ] `node tools/test-notebook.mjs` → **694 / 694 通过** → ⑤ 测试门禁

### ⑥ 不可行项（验证"确实不做"而不是坏掉）

- [ ] 全局找"AI 润色 / 转写 / 出题 / 手写搜词"入口 → 预期只有"本地摘要"与"规则式生成闪卡"两个替代品，文档写明原因 → ⑥ AI
- [ ] 检查网络请求：除 `api.github.com` 与 KaTeX CDN，页面不向第三方发请求（Token 只在 localStorage）→ ⑥ 隐私边界
- [ ] 两个窗口同时编辑同一本并保存 → 第二个保存前出现"远端被改过"提示，不静默覆盖 → ⑥ 协作（替代）
- [ ] 手机「添加到主屏幕」→ 断网仍可打开已缓存内容（`sw.js`）→ ⑥ 离线（iCloud 同步的替代）
- [ ] 分享只读链接 `#/book/<id>` 给另一台设备打开 → 笔记本可读 → ⑥ 分享（替代）

---

## 6. 按状态统计

> **统计口径**：只数各能力表里「状态」那一列，按最后一个单元格的 emoji 计数（`node -e` 脚本逐行核对，见文末命令）。
> 下面这张表是**交付后**的真实数字。

| 状态 | 数量 | 含义 |
| --- | --- | --- |
| ✅ 完整复现 | 49 | 语义与 Goodnotes 等价，**本期已交付**：资料库与封面、多选批量、回收站、多页与缩略图、书签大纲、14 种纸张与自定义、4 笔型与压感、荧光笔拉直、橡皮 3 模式、套索两种选择 + 变换 + 层级、形状识别与填充、文本框 4 字体、尺子吸附、激光笔、放大窗自动推进、视口缩放、跨页撤销、贴纸与胶带、录音时间锚定、闪卡 Leitner 间隔重复、自写 PDF 导出、演示模式、**手写 OCR 搜索**、**图片裁剪**、跨设备可见的 PWA 与离线便携版、计划本模板 |
| 🟡 简化复现 | 13 | 能用但缺口明确：文件夹无自定义色/emoji、**模板本**（已内置 4 套 + 可另存自己的，但还不能导入外部的 GoodNotes 模板文件）、**手写转文字**（第十一批能做到「按行框原位替换成可编辑文本框 + 一步撤销」，第十二批把批量识别做成可续跑队列，但仍要手动点一次、依赖外部视觉模型，不是系统级实时转换）、**音频转写**（第八批已接 SiliconFlow ASR，需自备 Key 且音频会上传第三方）、**AI 转写/纪要**（同样接上了 ASR，但没有大模型做纪要润色）、**Apple Pencil 笔势**（双击已用「手写笔快速双击」等效，涂黑擦除/悬停仍未做）、胶带撕后不能原地还原（可撤销）、层级只置顶置底、PDF 目录树未解析、多窗口无应用内分屏、日历无双向同步、会议纪要靠模板 + 检索、Math Assist 只能读不能判对错 |
| 🟠 替代方案 | 11 | 换一条路且**已交付**：无限白板 → 白板模板 + 视口缩放；实时分享链接 → GitHub Pages 只读数据 + 导出 JSON + **Markdown 一键提交进全站检索**；AI 总结 / 出题 / 润色 → 本地抽取式摘要 `summarizeText` + 从文本生成闪卡；Math Assist 公式 → 文本 + KaTeX；云备份 / iCloud → **仓库同步（`sync.mjs`，带冲突判定与本机备份）+ 免令牌线上导入**；邮件导入 → 编辑器贴入 + 提交；Widget / 日历集成 → 桌面快捷方式 + 时间线视图 |
| ❌ 不可行 | 9 | 结构性受限：实时协作（无后端）、AI 对话式提问与动态出题（无大模型）、服务器端搜索、密码保护笔记本（静态托管无鉴权）、PDF 目录自动解析、AirPlay/HDMI 硬投屏（浏览器只能全屏）、iCloud 账号级端侧同步、应用内多窗口分屏、原生平台能力（Apple Pencil 悬停 / 压感直读 API、Widget、Live Text）。原「音频转写」两条已由第八批的外部 ASR 补上，移入 🟡 |
| **合计** | **82** | 覆盖官方 App Store 描述 + Goodnotes 6 博客的每一条，以及行业公认的界面能力 |

**状态口径**：数字由文末那条命令从表格「状态」列机械统计（当前 `✅49 / 🟡13 / 🟠11 / ❌9`），不是手写估计。
「与 Goodnotes 完全等价」才算 ✅，所以像「图片裁剪」「模板库」「手写转文字」「音频转写」这类**已交付但与原版仍有差距**的能力记在 🟡/🟠，并在说明里写清差在哪。

**一句话读法**：**49 项完整落地，13 项能用但缺口明确，11 项换路走且替代已交付，9 项结构性不可行（都写明了原因）。**
第十一批补的是「**把写下的东西变成可再加工的东西**」这一环：手写**按行框原位换成可编辑文本框**（一步撤销）、封面可以用**自己的图**、资料库直接给「**今天该复习**」入口、导出多两种「发出去就能看」的格式（**EPUB / 单文件 HTML**，均为浏览器里手写字节）。
第十二批把**手写识别**从一个按钮做成**跨本批量队列**：已识别的页与「确认没字」的页都不再重跑、失败页单独记账并可一键重试、密钥错/余额不足立刻停、每页完成即落盘所以随时能续跑。
第十三批把**闪卡**做成**复习中心**：按卡片标签 / 笔记本标签 / 单本筛着复习（筛选按钮上直接写还剩几张）、复习热图与连续天数、以及导出 Anki 可导入的 CSV（把间隔重复的数据带出站点）。
第十四批把**EPUB 导出**做成一本书：**书封**（自己的图或按配色自动画）、**层级目录**（标题一级 / 书签二级，EPUB3 的嵌套 ol + EPUB2 的 NCX 都写）、正文「见第 3 页」变**书内链接**；测试还会解开自己写出的 ZIP 检查**没有死链**。
第十五批把**打印**这件事收尾：**省纸排版**——2 页拼一张（横版 / 竖版）与**骑马钉小册子**（A3 双面折成 A4，页序自动重排），拼版靠 PDF 坐标系变换完成，所以可搜索文字层、书签目录、页内链接全都跟着走。
第二十二批加**页面总览**：一屏网格看完所有页，点选 / Shift 连选后可批量删除、复制、移到最前/最后、加去书签、导出选中页 PDF（双击某页跳过去）。
第二十一批把**卡片来源**做成可维护的：全库报告分「没来源 / 来源页已删」两类，能按卡面文字**唯一命中才自动找回**、也能批量清掉失效来源；同批修掉「新建笔记本填了名字却建不出来」（标题只存在输入框里、从没同步进状态，点一下颜色就被静默清掉）。
第二十批加**打印检查单**：导出前先算「几页 → 几面 · 几张纸 · 约多少 MB」，并列出空白页 / 只有胶带 / 没识别 / 内容密这四类值得注意的页（可点页号跳过去）；看完可开「跳过空白页」导出，书签、目录页码与页内链接会一起重编号。
第十九批让 **PDF 书签跳到页内位置**：能对上页标题的那一行（或该页最靠上的内容）成为落点，书签直接滚到那一行（靠左的内容横向不动、拼版时落点跟着缩放）。
第十八批补上**跨页拖拽**：把选中的对象直接拖到左侧缩略图上就换页，落页一步撤销（两页同时复原）。
第十七批把**复习卡与原文接起来**：卡面显示「来自《本名》第 N 页」并可「去看原页」（回来还停在同一张卡），来源页被删时能识别出来并「清掉来源」。
第十六批补上**识别质量回看**：机械判「字数异常少 / 疑似乱码 / 内容重复 / 缺少行位置 / 行太碎 / 上次失败」，把值得看的页列成清单（可「去看看」跳到原页），并支持**一键只重跑这些页**（含「重跑并补行位置」）——形成「识别 → 抽查 → 重跑 → 复判」的闭环。
第五~六批重点补了三处：**PDF 带可搜索文字层且按 OCR 行位置对齐**（Ctrl+F 能搜手写、能选中复制）、**模板库**（内置 4 套 + 另存自己的）、**导出 Markdown 并一键进全站检索/知识图谱**。
第三批交付把两个大缺口补掉了：**手写现在能搜了**（Qwen3-VL OCR → 进全文检索 → 还能转成可编辑文本），**笔记本能进 git 了**（推送/拉取/免令牌只读导入，冲突规则明确）；顺带做了**图片裁剪**与**手写笔双击切工具**。
剩下的差距全部落在「需要后端 / 需要模型 / 需要原生 App」这三类里，编辑器手感这一层已经对齐。
统计命令（在本仓库根目录跑，可随时复核）：

```bash
node -e "const fs=require('fs');const s=fs.readFileSync('文档-GoodNotes-功能对照与复现.md','utf8');let tbl=null;const ts=[];for(const l of s.split('\n')){if(/^\|/.test(l)){const c=l.split('|').slice(1,-1).map(x=>x.trim());if(!tbl){tbl={h:c,r:[]};ts.push(tbl)}else if(!/^[-: ]+$/.test(l.replace(/\|/g,'').trim()))tbl.r.push(c)}else tbl=null}const n={ok:0,part:0,alt:0,no:0};for(const t of ts){const i=t.h.findIndex(h=>/状态/.test(h));if(i<0)continue;for(const r of t.r){const c=r[i]||'';if(/✅/.test(c))n.ok++;else if(/🟡/.test(c))n.part++;else if(/🟠/.test(c))n.alt++;else if(/❌/.test(c))n.no++}}console.log(n)"
```
